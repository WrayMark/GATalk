# GATalk 0.15.0 工作区生产基础验证记录

日期：2026-08-08  
状态：内部候选通过，等待真实项目试用

## 交付范围

- 项目诊断、恢复点、目录修复和移动项目重定位。
- 已登记工作区的全局检索与对象定位。
- 制作阶段、任务依赖、批量状态和阶段质量门禁模板。
- 资产生产规格、依赖顺序与 JSON/CSV 交接。
- 视觉资料板、连线、快照恢复和 PNG 导出。

## 自动化验证

- 完整测试：270 项通过。
- 覆盖：项目类型识别、安全目录修复、恢复点、工作区重定位、全局检索、
  审阅中心写锁、任务循环依赖、门禁模板去重、资产依赖顺序、交接导出、资料板
  保存/快照/删除清理，以及相关界面构造。
- 默认测试完全离线，没有调用真实 AI 服务。

## Windows 构建

- PyInstaller `onedir` 构建成功。
- 程序：`dist/GATalk/GATalk.exe`。
- 文件：232 个，总计 256.8 MiB。
- 打包内离线烟测：100%、125%、150% 缩放退出码均为 0。
- 真实高 DPI 显示器和无 Python 干净机仍是 Windows Alpha 人工项。

## 界面检查

已生成并检查以下实际运行截图：

- 工作台首页；
- 视觉资料板；
- 资产生产交接；
- 审阅任务与质量门禁；
- 全局检索；
- 项目诊断与恢复。

截图位于 `.artifacts/screenshots-0150`，不进入发布提交。

## 使用手册检查

- `USER_GUIDE.md` 已更新到 0.15.0。
- `GATalk_使用手册.docx` 已从 Markdown 权威来源重建。
- DOCX 内容断言、标题层级、页边距、编号结构和无障碍审计通过；审计没有高、
  中、低级问题。
- 当前机器没有 LibreOffice；Microsoft Word 无界面 PDF 导出被 Office 环境阻塞，
  因此本轮未完成页面 PNG 视觉渲染。Word 文件本身可打开性仍需用户在实际机器
  上确认。

## 人工试用步骤

1. 启动 `dist/GATalk/GATalk.exe`，从首页按 `Ctrl+K` 搜索已打开项目和资料。
2. 打开“项目诊断”，检查一个项目并建立恢复点；复制该项目后再测试恢复。
3. 在审阅中心应用“白盒”或“灯光初版”门禁模板，新建两个任务并设置前置关系。
4. 在资产拆分打开已有方案，进入“生产交接”，同步规格、修改一项并导出
   JSON/CSV。
5. 在资料库建立视觉资料板，加入图片和笔记，连接、移动、快照、恢复并导出 PNG。
6. 关闭程序后重开，确认任务、门禁、生产规格和资料板全部恢复。

## 已知边界

- 全局检索只读取用户已经打开过的 GATalk 工作区，不扫描全盘或外部网页。
- 恢复工具不会补造丢失原图或数据库；自动修复只处理安全基础目录。
- 生产规格是人工确认的计划，不代表从单张原画获得真实尺寸、枢轴或不可见结构。
- 视觉资料板当前不支持多人协作、云同步或第三方白板格式。
