# SceneLens 产品简述

状态：已确认  
日期：2026-07-18

## 1. 产品定位

SceneLens 是面向游戏场景美术、UE 地编和环境艺术作品的 Windows 本地视觉
审阅工具。它不是通用图片编辑器，也不以 AI 代替美术判断。

核心工作流：

> 参考目标 → 当前截图 → 可复现分析 → 有依据的诊断 → 修改任务 → 新版本复查

核心价值是审阅闭环、证据定位和历史追踪，而不是滤镜数量或综合评分。

## 2. 目标用户

- 游戏环境美术与场景美术
- Unreal Engine 地编与灯光人员
- 需要将概念目标与实时场景截图持续对照的个人创作者

当前首先服务个人专业使用，未来保留对外发布或出售的可能。

## 3. 核心数据关系

正式架构采用：

> Project（项目）→ Shot（审阅镜头）→ Version（截图版本）

一个项目可以有多个镜头。只有同一镜头、稳定机位下的版本才允许进行透明
叠加、滑块、像素差分或 SSIM。概念图与未对齐截图只进行视觉关系比较。

## 4. Art Brief

项目或镜头必须记录 Art Brief，至少包含：

- 项目类型
- 当前制作阶段
- 目标风格
- 时间与天气
- 目标情绪
- 主要视觉焦点
- 希望保留的内容
- 当前最困扰的问题
- 暂时不需要审阅的内容
- 制作条件与限制

算法解释和 AI 审阅必须参考制作阶段与 Art Brief。例如白盒阶段不应重点
批评材质精度。

## 5. 信息分层

每条结果必须标记为以下一种：

1. **测量结果**：像素、色彩、明度等直接统计。
2. **算法推断**：基于参数与启发式算法得出的关系。
3. **美术判断**：结合 Art Brief、画面意图和经验形成的解释。

不使用缺乏依据的综合美术评分。

## 6. V0.1 保留范围

- 项目、镜头、版本与只读图片导入
- 参考图与 UE 截图并排比较、A/B、同步缩放和平移
- 同镜头版本的透明叠加与滑块比较
- 灰度、模糊、三阶/五阶明度、明度直方图
- Oklab 色板与比例
- 色相、明度、饱和度分布
- 矩形区域分析
- 修改任务
- AI 审阅包导出与 JSON 导入
- 同镜头前后版本复查

## 7. V0.1 明确推迟

- 视频、EXR、HDR、ACES
- UE 插件、UE 工程扫描、自动修改场景
- 自动显著性热图、自动冷暖结论、色板角色断言
- 自由多边形选择、完整报告编辑器
- Markdown 报告解析
- 多张 UE 截图同时分析
- 高级边缘密度和局部反差
- Depth Anything、SAM 2、CUDA 和本地大型视觉模型
- 在线账户、云同步、多人协作

## 8. 隐私原则

- 软件不自动上传图片或项目数据。
- 外部 AI 审阅只能由用户主动导出。
- 导出支持去元数据、降分辨率、隐藏项目名和本地路径。
- 商业保密内容是否可上传由用户依据所属团队政策决定。

## 9. 图像安全与兼容

- 永不覆盖用户原始文件。
- 导入后保留原始字节；派生结果进入 `artifacts`。
- 分析产物可删除并重新生成。
- 支持中文项目名、中文用户目录、空格路径。
- 测试 125%/150% DPI、2K/4K、PNG/JPG/JPEG/WebP、EXIF 方向和 ICC。

