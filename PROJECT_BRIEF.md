# GATalk 产品简述

状态：已确认  
日期：2026-07-18

## 1. 产品定位

GATalk 是面向游戏场景美术、UE 地编和创作研究的 Windows 本地工作台。
它由跨业务的资料平台和面向明确任务的专业工作台组成，不是通用图片编辑器，
也不以 AI 代替美术判断。

核心工作流：

> 制作目标 → 参考图 → 当前截图 → 证据测量 → AI 专项审阅 → 修改任务 →
> 优化预演 → 新版本复查 → 质量门禁

核心价值是审阅闭环、证据定位和历史追踪，而不是滤镜数量或综合评分。

当前内置工作台包括：

- 场景美术控制：参考、当前 UE 截图、证据审阅、任务和版本复查。
- 作品研究：面向单张优秀作品的学习拆解。
- 资产拆分：把场景原画转成可校正的资产套件规划、概念图和展示板。
- 作品研究集合与对照研究：把 2 至 6 件作品放在同一问题与维度下研究。

“参考资料与知识库”位于专业工作台之上，管理跨任务复用的原画、ArtStation
来源、文章、教程、项目笔记、标签、译文、局部摘录与项目引用。当前启用美术
参考资料域；未来关卡设计、策划等资料域通过注册边界加入，不要求改写场景美术
业务核心。

“审阅任务与质量门禁中心”同样位于平台层。它接收场景美术控制、作品研究和
资产拆分中由用户确认的结论，记录来源与验收条件，并在后续版本中复查是否改善、
解决或仍缺少证据。专业项目仍拥有原始分析和业务数据，审阅中心只保存可追溯的
跨项目任务索引和版本历史。

作品研究与资产拆分可通过用户主动触发的交接快照连接。资产拆分先理解场景，
再由用户选择逐类别拆分深度；同一原画可以保存多个不同制作目的的方案。

## 2. 目标用户

- 游戏环境美术与场景美术
- Unreal Engine 地编与灯光人员
- 需要将概念目标与实时场景截图持续对照的个人创作者

当前首先服务个人专业使用，未来保留对外发布或出售的可能。

## 3. 核心数据关系

正式架构采用：

> Project（项目）→ Shot（审阅镜头）→ Version（截图版本）

一个项目可以有多个镜头。只有同一镜头、稳定机位下的版本才允许进行透明
叠加、滑块、像素差分或 SSIM。概念图与未对齐截图只进行视觉关系比较。

## 4. 制作意图（Creative Intent）

原 Art Brief 在中文界面统一显示为“制作意图”。它属于 Project，Shot 可以
覆盖部分字段，用于说明用户希望场景最终呈现什么，至少包含：

- 场景类型
- 当前制作阶段
- 目标风格
- 时代或世界观
- 时间、季节与天气
- 目标情绪
- 第一与次要视觉焦点
- 希望保留的内容
- 当前最困扰的问题
- 暂时不需要审阅的内容
- 制作条件与限制
- 自定义补充说明

算法解释和 AI 审阅必须参考制作阶段与制作意图。例如白盒阶段不应重点
批评材质精度。

## 5. 信息分层

每条结果必须标记为以下一种：

1. **测量结果**：像素、色彩、明度等直接统计。
2. **算法推断**：基于参数与启发式算法得出的关系。
3. **美术判断**：结合 Art Brief、画面意图和经验形成的解释。

不使用缺乏依据的综合美术评分。

## 6. V0.1 保留范围

- 项目、镜头、版本与只读图片导入
- 参考图与 UE 截图并排比较、A/B、同步缩放和平移
- 同镜头版本的透明叠加与滑块比较
- 灰度、模糊、三阶/五阶明度、明度直方图
- Oklab 色板与比例
- 色相、明度、饱和度分布
- 矩形区域分析
- 修改任务
- AI 审阅包导出与 JSON 导入
- 同镜头前后版本复查

## 7. V0.1 明确推迟

- 视频、EXR、HDR、ACES
- UE 插件、UE 工程扫描、自动修改场景
- 自动显著性热图、自动冷暖结论、色板角色断言
- 自由多边形选择、完整报告编辑器
- Markdown 报告解析
- 多张 UE 截图同时分析
- 高级边缘密度和局部反差
- Depth Anything、SAM 2、CUDA 和本地大型视觉模型
- 在线账户、云同步、多人协作

## 8. 隐私原则

- 软件不自动上传图片或项目数据。
- 外部 AI 审阅只能由用户主动导出。
- 导出支持去元数据、降分辨率、隐藏项目名和本地路径。
- 商业保密内容是否可上传由用户依据所属团队政策决定。

## 9. 图像安全与兼容

- 永不覆盖用户原始文件。
- 导入后保留原始字节；派生结果进入 `artifacts`。
- 分析产物可删除并重新生成。
- 支持中文项目名、中文用户目录、空格路径。
- 测试 125%/150% DPI、2K/4K、PNG/JPG/JPEG/WebP、EXIF 方向和 ICC。

## 10. M1B 参考理解关系

M1B 建立三方关系：

> 制作意图 → 参考图呈现的视觉特征 → 当前 UE 截图的实际效果

每张主要参考图绑定独立的“参考图视觉简报”，记录参考用途、时间环境、
构图层级、Color Key、灯光氛围和内容制作观察。自动测量、算法推断、AI
分析、用户填写和用户修订必须分别标记来源；用户确认或修订过的字段具有
最高写入优先级。

GATalk 长期采用“模块化单体，预留插件能力”。本阶段只建立稳定模块身份、
分析器接口、模块配置和模块数据版本边界，不实现插件发现、插件市场或未知
未来模块。

M1B.2 使用成对区域解决参考图与 UE 截图无法像素对齐的问题。用户分别框选具有
相同视觉职能的矩形，再比较局部明度、Oklab、彩度、色相和共享色板组成。参考
区域绑定 Shot，当前区域绑定具体 Version；系统只呈现测量和算法推断，不自动
生成美术好坏判断。

## 11. M2/M3 控制工作台边界

M2 引入可插拔供应商能力、主美与灯光专项审阅、AI 证据校验、结构化任务和质量
门禁。无 API Key 时，项目、测量、区域、任务、报告和离线审阅包仍完整可用；
网络发送只能由用户主动触发。

M3 引入目标匹配画像和两种优化预演。本地安全调色保持几何、只读原图且严格可
复现；生成式结果保存为 `AIConceptPreview`，不能自动成为真实 UE 截图
Version。结构漂移或保护区变化过大时，只能作为概念参考。

## 12. M4 深度审阅边界

M4 将主美审阅扩展为八维证据化会诊。AI 必须先理解制作阶段和用户目标，再分别
说明参考图呈现与当前截图效果；最多提出五个核心问题，并输出保留项、执行顺序、
不确定性和下一版验证方法。构图、材质和叙事等属于美术判断，不能伪装成像素
测量。

本地证据摘要只提供明度、反差、彩度、三阶比例和九宫格空间统计。它不是显著性
热图，也不能从 LDR 截图恢复 Lux、EV、材质参数或 UE Actor。AI 的可测声明会由
原始像素复核；冲突结论继续显示并降低可信度。

七类构图辅助线是纯画布观察工具，不自动评价构图好坏。M4 不引入新模型、CUDA、
自动分割或隐式联网。

## 13. M5 作品研究

GATalk 从单一场景审阅界面演进为应用外壳加多个内置工作区。启动后先选择：

- **场景美术控制**：原有制作目标、参考图、UE 截图、证据、任务和版本闭环。
- **作品研究**：面向单张原画、概念图、Color Key、环境作品或优秀截图的学习
  拆解与评价。

作品研究不是“只有一张图的对比分析”。它按近距离描述、形式关系、观看效果、
解释、评价与迁移学习的顺序工作，覆盖构图、层级、明度、色彩、光、空间、形状、
边缘细节、材质、环境叙事、风格技法和情绪十二维，并要求说明跨维度因果关系。

本地分析只提供可复核的明度、Oklab、彩度、边缘和空间网格证据。注意力代理是
反差、边缘密度和彩度的启发式组合，不等同于眼动、语义显著性或构图好坏。
AI 研究区分画面证据、本地测量、专家推断与背景假设，不虚构作者意图、焦距、
真实灯光参数或制作过程，不使用总分。

## 14. M6 资产拆分工作台

第三个内置工作区把复杂场景原画转换为可校正、可追溯的生产资产计划：

- 建筑、模块构件、道具、植被、地形、材质、贴花、远景和灯光特效代理；
- 父子层级、重复与变体、复用组、制作优先级和拆分依据；
- 原图归一化区域、可见像素近似遮罩和用户校正；
- 只对用户勾选项生成独立概念图、保守遮挡补全图和展示图；
- 导出单项图片、资产展示板和结构化清单。

原画直接可见证据、AI 对资产结构的推断和 AI 生成补全分开保存。不可见背面、
内部结构、精确尺寸和材质节点不能写成确定事实。原图继续只读，所有网络操作
必须查看清单并由用户主动确认。

该模块与 Shot/Version 场景审阅和单图作品研究相互独立，只复用工作区注册、
Provider、凭据、图片读取、原子存储和通用画布。当前不捆绑本地视觉大模型、
PyTorch、CUDA 或模型权重，也不生成生产可用三维模型。

## 15. 全自动资产板

- 资产拆分提供三条并列流程：可校正拆分、全自动资产板与资产拆分提示语。
- 全自动流程从主原画独立生成资产清单、单项概念图和一张资产展示板，不修改人工校正清单。
- 每次运行保存来源图片哈希、资产区域、可见遮罩、供应商、模型、参数、生成结果和错误。
- 批量远程调用只能由用户主动触发，发送前显示最大调用数量与费用风险；系统性故障立即停止后续调用。
- 自动生成仍属于概念辅助，不是原画中不可见结构的事实，也不是生产可用三维资产。

## 16. 资产拆分提示语

- 提示语流程独立于资产清单和全自动生成记录，但可保存在同一资产项目。
- AI 初稿可读取主原画、补充参考、场景类型、制作目标和用户备注。
- 后续协商以当前提示语和用户反馈为主，默认不重复发送图片；用户可主动选择
  重新附图。
- 中文、英文、负面提示和硬约束都可手动编辑；每次 AI 或手动修改保存为新版本。
- 直接可见证据与不可见推测分开表达，不把补全内容伪装成原画事实。
- 提示语用于复制到外部图像生成工具；GATalk 不自动代表用户提交到外部网站。

## 17. 工作区生产基础

GATalk 的平台层负责跨专业工作区的共用操作，但不吸收各模块的业务数据：

- 全局检索只索引已经打开过的本地工作区，并返回具体项目与对象；
- 项目诊断与恢复只处理入口、数据库、目录和恢复点，不猜测丢失业务内容；
- 审阅中心统一接收确认任务、制作阶段、依赖、版本复查和质量门禁；
- 资产生产规格把拆分结果转换为可交接计划，但不直接修改 UE 工程；
- 视觉资料板属于参考资料库，用于整理来源、关系和研究方向。

平台层继续本地优先、无隐式联网。跨模块跳转使用稳定对象身份；专业算法、
资产拆分规则和资料板内容仍由各自模块保存。

## 18. 证据对比与审阅追溯

- 场景审阅的对比分析同时呈现参考图与当前截图的同尺度分布曲线、独立色板、
  共享色板和三阶明度关系；用户可按明度、彩度、色相或饱和度切换观察维度。
- 灯光审片按曝光与明度范围、主辅光、焦点层级、空间分离、色温、阴影与剪影、
  氛围体积和游戏可读性八个维度组织证据，不使用泛化总分。
- 场景审阅、作品研究和对照研究默认保存每次 AI 审阅结果；界面默认显示最新
  结果，同时允许查看和删除历史运行。
- 所有专业工作台共享同一套第一层导航，全局命令与模块命令分行呈现。

## 19. 语言与国际发行边界

GATalk 的界面语言是应用外壳能力，所有工作台共享同一设置。简体中文是基准源语言；
首批目标为繁体中文、英语、日语和法语。语言选择保存在本机，
不会写入项目或触发联网。

界面译文与用户内容严格分开。切换语言不翻译项目名称、研究笔记、来源文档或历史
AI 结果；新 AI 任务可以要求按当前语言输出。商业发行前必须完成母语审校、专业术语
冻结、控件长度和高 DPI 截断测试。德语、西班牙语、韩语、小语种和运行时下载
语言包暂不进入当前范围。
