# GATalk 0.10.0 品牌、全局设置与界面验证

日期：2026-07-31  
候选标签：`v0.10.0-gatalk-brand-settings-ui`

## 完成范围

- 用户可见产品名、窗口标题、首页和 Windows 构建产物改为 GATalk。
- 全局设置支持跟随 Windows、浅色、深色、四种强调色、字号、控件密度、
  窗口与停靠面板布局记忆。
- 工作台首页重做；三个既有工作台共用菜单、工具栏、停靠面板、输入、表格、
  页签、滚动条、主要操作和状态颜色。
- 旧项目格式、模块 ID、数据库、项目扩展名、最近项目和 Windows 凭据兼容。

## 自动化验证

- 完整离线测试：228 项通过。
- 新增覆盖：设置 JSON 往返、中文路径、损坏/高版本设置回退、布局清除、
  深浅主题差异、设置窗口、快捷键、窗口布局保存和旧最近项目列表迁移。
- Python 编译检查通过。
- PyInstaller `onedir` 构建通过。
- 包内 `GATalk.exe --smoke-test` 退出码 0；未使用 API Key 或联网。

## 构建结果

- 入口：`dist/GATalk/GATalk.exe`
- 文件数：229
- 总大小：268,996,716 bytes，约 256.5 MiB
- EXE 大小：8,953,468 bytes
- EXE SHA-256：
  `7C3C85FC287940ADA8BEF0A07F5D8D1C390B3464254A6F847FC9C673690D2784`

## 实际渲染检查

- 深色首页：
  `docs/images/gatalk-0.10.0/gatalk-hub-dark-0.10.0.png`
- 浅色首页：
  `docs/images/gatalk-0.10.0/gatalk-hub-light-0.10.0.png`
- 全局设置：
  `docs/images/gatalk-0.10.0/gatalk-global-settings-0.10.0.png`

检查了中文文本、卡片层级、主要按钮、浅色/深色对比、设置控件、窗口缩放和
无标签实色底块。旧工作台功能回归均通过。

## 人工试用步骤

1. 启动 `GATalk.exe`，检查首页显示三个工作台。
2. 打开“全局设置”，依次切换跟随 Windows、浅色和深色。
3. 切换强调色、字号和控件密度，点击“应用”确认三个工作台同步变化。
4. 调整工作台窗口、停靠面板和工具栏，关闭再打开，检查布局恢复。
5. 点击“清除已保存布局”，重新打开工作台检查恢复默认布局。
6. 打开一个旧 `.scenelens`、`.scenelens-study` 和 `.scenelens-assets`
   项目，检查图片、数据与 API Key 无需重建。

## 尚需人工确认

- “跟随 Windows”随真实 Windows 设置即时切换的体验。
- 125%、150% 和多显示器 DPI 下的主观字号与间距。
- 旧 Windows 凭据在真实系统凭据管理器中的一次性迁移。

这些项目不改变数据结构或算法正确性，保留为 Windows 人工体验检查。
