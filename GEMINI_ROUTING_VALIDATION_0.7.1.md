# Gemini 模型路由修复验证记录

候选版本：0.7.1  
日期：2026-07-30

## 故障结论

真实资产拆分请求先遇到 `gemini-3.5-flash` 暂时繁忙，随后 SceneLens 回退到
`gemini-2.5-flash`。后者返回 404，并明确表示不再向新用户开放。因此本次
故障属于 SceneLens 的备用模型配置过期，不是 API Key 错误。

## 修复

- 默认模型：`gemini-3.6-flash`。
- 备用模型：`gemini-3.5-flash`、`gemini-3.5-flash-lite`。
- 主模型 404/503 后按清单尝试备用模型。
- 单个备用模型 404/503 后继续下一项。
- 400、401、403、429 等错误不回退，不跨供应商。
- 发送确认展示完整备用链和潜在额外费用。
- 失败信息记录脱敏后的模型尝试路径和各模型错误代码。

## 自动验证

- Provider Manifest 默认模型和备用顺序。
- 主模型 404 与 503 两种路径。
- 第一个备用模型 404、第二个备用成功。
- 全部模型失败时的中文提示与完整路由。
- 400 参数错误不错误触发回退。
- 发送确认显示完整备用链。
- 默认测试完全离线，不读取系统凭据，不消耗 API 额度。
- 完整自动化测试：207 项通过。
- 源码与 Windows `onedir` 包内烟测退出码均为 0。
- `onedir`：228 个文件，268,916,705 bytes（约 256.5 MiB）。
- 主程序 SHA-256：
  `6CFB41E0DFD2E3BC317049ACBA12410C514A053B24C54D61EE497E85840BB205`。
- 已检查包内 `providers.json`：包含新默认模型和双备用链，不包含旧
  `gemini-2.5-flash`。

## 人工复测

1. 完全退出旧版 SceneLens。
2. 启动本次 `0.7.1` 候选目录中的 `SceneLens.exe`。
3. 进入“资产拆分工作台”，确认模型 ID 默认显示
   `gemini-3.6-flash`。
4. 导入原画，点击 AI 资产拆分。
5. 在发送确认中检查两个备用模型。
6. 主动确认发送并观察完成状态中的实际模型。
7. 若仍失败，保留错误窗口；不要发送 API Key。

真实联网结果受账号权限、地域、网络和供应商容量影响，不能由离线测试伪造。
