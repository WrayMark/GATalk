# SceneLens 持续开发规则

## 当前阶段

- M0.5 已于 2026-07-19 通过人工验收并冻结。
- M1A 已于 2026-07-19 通过真实项目人工验收并冻结。
- 当前阶段是 M1B.0：多进程保护、扩展性边界、Brief 数据模型、schema
  迁移和预设配置。
- M1A 试用通过前，不实现 M1B 色板遮罩、明度比较、色板比较和成对区域。
- 仍不实现 AI 审阅、正式报告、视频、模型或隐式联网。

## 架构边界

- `analysis` 只处理 NumPy 数据，不导入 PySide6。
- `imaging` 负责读取、EXIF、ICC、sRGB 和 NumPy/QImage 边界。
- `ui` 只负责交互和呈现，通过清晰接口调用分析层。
- UI 不包含色彩转换、聚类或明度算法实现。
- 正式数据层放在 `storage`，不得让 UI 直接拼接 SQL。
- `storage` 不导入 PySide6；迁移、原子写入与业务存储接口分别测试。

## 图像规则

- 永不覆盖或重新保存用户输入文件。
- Pillow 读取并执行 EXIF 方向修正。
- 有 ICC 时使用 ImageCms 转为 sRGB；无 ICC 时明确记录为“假定 sRGB”。
- 计算明度时使用线性 sRGB 相对明度，不直接平均 gamma 编码 RGB。
- Oklab 聚类必须固定随机种子并记录参数。
- 派生图应可删除并重建。

## UI 与性能

- 所有耗时图片读取和分析必须离开 GUI 线程。
- 只在 GUI 线程创建 QPixmap。
- 4K 图片使用有界分析采样，不能把全部像素直接送入聚类。
- Qt6 默认高 DPI；不得写死依赖 100% 缩放的像素布局。
- 所有路径使用 `pathlib.Path`，不得假设 ASCII 或无空格路径。

## 质量门槛

- 每个分析算法必须有基础单元测试。
- 色彩转换、EXIF、ICC、三阶/五阶明度必须有确定性测试。
- 可见功能完成后运行相关测试；阶段结束运行完整测试。
- 打包后必须执行至少一次启动烟测。
- 错误信息面向非程序用户，同时将技术细节写入日志。

## 依赖与许可证

- 不加入范围外的模型、CUDA、服务端框架或大型 UI 依赖。
- 新依赖必须说明用途、许可证、Windows wheel 和打包影响。
- 依赖升级必须运行算法、UI 和 PyInstaller 验证。
- `THIRD_PARTY_LICENSES.md` 与依赖变更同步更新。

## 文档与决策

- 重要产品或技术取舍写入 `DECISIONS.md`。
- 阶段范围和验收变化写入 `ROADMAP.md`。
- 用户可见变化写入 `CHANGELOG.md`。
- 文档用中文；代码标识符、类型和测试名称使用英文。

## 常用命令

```powershell
.\start_dev.cmd
.\scripts\test.ps1
.\build_alpha.cmd
```
