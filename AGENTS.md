# SceneLens 持续开发规则

## 当前阶段

- M0.5 与 M1A 已于 2026-07-19 通过人工验收并冻结。
- M1B.0 已完成并建立 `v0.2.0a0-m1b0` 回退点。
- M1B.1 已通过用户真实项目试用并冻结。
- M1B.2 已通过用户真实项目试用并冻结。
- M2“AI 主美控制台与灯光审片台”内部候选已完成并建立第一个重大外部试用点。
- M3“参考匹配与画面优化实验室”内部候选已完成；当前停在第二个重大外部
  试用点，等待真实项目反馈后再决定 M4 范围。
- 不实现本地大型模型、视频、CUDA、自由多边形、动态第三方代码加载、插件市场
  或隐式联网。

## 模块与依赖边界

- 采用模块化单体；当前业务模块 ID 为 `scenelens.visual_review`。
- `core` 只保存真正跨模块复用的契约，不依赖具体业务模块。
- `modules/<module_id>` 可拥有分析器、配置、业务服务、数据表和未来 UI 入口。
- `analysis` 只处理 NumPy 数据，不导入 PySide6。
- `imaging` 负责读取、EXIF、ICC、sRGB 和 NumPy/QImage 边界。
- `ui` 只负责交互和呈现，不实现色彩转换、聚类、明度算法或 SQL。
- `storage` 不导入 PySide6；迁移、原子写入和业务存储接口分别测试。
- 新分析器经统一注册表接入，并记录模块、分析器、版本、参数和输入哈希。
- 不为尚未实现的未来模块制造抽象；发现第三个真实调用方后再提取公共能力。

## 数据与并发

- `project.json` 是项目入口，`project.db` 保存结构化业务数据。
- 模块业务表使用模块前缀并记录模块 schema 版本。
- schema 迁移前必须备份数据库和清单；高版本项目拒绝写入。
- 同一项目同一时间只能有一个写进程；其他进程只读打开或取消。
- 操作系统文件锁是写权限依据，不能只凭 PID、时间戳或锁文件存在判断。
- 自动、算法或 AI 内容不得覆盖用户填写、修订或确认过的 Brief 字段。

## 图像规则

- 永不覆盖或重新保存用户输入文件；导入保留原始字节和 SHA-256。
- Pillow 执行 EXIF 方向修正；有 ICC 时用 ImageCms 转 sRGB，无 ICC 时记录
  为“假定 sRGB”。
- 明度使用线性 sRGB 相对明度，不平均 gamma 编码 RGB。
- Oklab 聚类固定随机种子并记录参数；派生图可删除并重建。

## UI 与性能

- 耗时读取和分析离开 GUI 线程，只在 GUI 线程创建 QPixmap。
- 4K 图片使用有界分析采样，不把全部像素直接送入聚类。
- Qt6 高 DPI 布局不得写死依赖 100% 缩放的像素尺寸。
- 路径使用 `pathlib.Path`，不得假设 ASCII 或无空格。
- 快速切换 Shot/Version 时，过期后台任务不得覆盖当前结果。
- API 与图像编辑任务必须在后台执行，支持取消、超时、有限重试和错误脱敏。
- 所有网络发送必须由用户主动触发，并在发送前展示数据清单。

## 质量门槛

- 每个分析算法有基础单元测试。
- 色彩转换、EXIF、ICC、三阶/五阶明度和固定种子有确定性测试。
- 可见功能后运行相关测试；阶段结束运行完整测试。
- 每阶段独立 Git 提交并保留稳定回退点。
- M2/M3 内部纵向切片使用原子提交和可回退标签；只在两个重大试用点暂停。
- 每阶段构建 `onedir`，并至少执行一次打包启动烟测。
- 用户错误信息使用非程序化语言，技术细节写入日志。

## 依赖、文档与许可证

- 不加入范围外模型、CUDA、服务端框架或大型 UI 依赖。
- 新依赖说明用途、许可证、Windows wheel 和打包影响。
- 依赖升级运行算法、UI 和 PyInstaller 验证。
- 重要取舍写入 `DECISIONS.md`，范围写入 `ROADMAP.md`，用户变化写入
  `CHANGELOG.md`，依赖变化同步 `THIRD_PARTY_LICENSES.md`。
- 文档使用中文；代码标识符、类型和测试名称使用英文。

## 常用命令

```powershell
.\start_dev.cmd
.\scripts\test.ps1
.\build_alpha.cmd
```
