# M1B.1 工程验证记录

状态：试用候选  
日期：2026-07-19  
版本：`0.2.0a1`

## 1. 已实现闭环

> 制作意图 → 参考图视觉简报 → 参考/当前全图测量 →
> 共享色板与三阶明度差异 → 颜色来源遮罩 → 保存重开

包含：

- 项目级制作意图和 Shot 覆盖数据入口；
- 可编辑单选预设、多选预设和任意自定义值；
- 参考图视觉简报六组语义字段；
- 图片尺寸、宽高比、格式、ICC、Oklab 色板、明度、三阶/五阶、色相和
  饱和度自动关联；
- 共享 Oklab 色板、双方占比和百分点差；
- 独立色板单图遮罩、共享色板双图遮罩、再次点击/Esc 退出；
- 可调低/高阈值的三阶明度比例、差异和双方缩略图；
- 活动页签、阈值、原有显示/缩放参数和分析结果恢复。

## 2. 数据与迁移

- schema v3 增加模块对比分析缓存。
- schema v2 → v3 和 schema v1 → v3 都在迁移前备份。
- 对比记录保存模块、分析器、版本、参数、双方输入 SHA-256 和证据类型。
- 参考图变化后旧视觉简报和对比分析标记过期。
- Reference Visual Brief JSON Schema 已随应用资源保存；本阶段不实现 AI
  包导出或 JSON 导入。

## 3. 自动化验证

- [x] 原有 M0.5 与 M1A 测试继续通过。
- [x] 完整测试 47 项通过。
- [x] 预设与自定义值保存恢复。
- [x] 字段来源、用户确认和自动覆盖保护。
- [x] 共享色板双方严格等量采样并固定种子复现。
- [x] 遮罩按已保存中心分类，不重新聚类。
- [x] 快速取消遮罩和过期对比任务不能覆盖当前状态。
- [x] M1B.1 创建、分析、遮罩、保存、关闭和重开 UI 纵向测试。
- [x] Windows `onedir` 构建，目录大小约 253.9 MiB。
- [x] 打包内部烟测退出码为 0。
- [x] 预设和 Reference Visual Brief JSON Schema 已随包收集。

## 4. 离屏视觉检查

- 1500 × 900 离屏窗口下，左侧导航、双画布、共享色板、明度表和缩略图
  无阻断性重叠。
- 对比分析页可滚动，右侧通过 splitter 调整宽度。
- 该检查不能替代真实 125%/150% 高 DPI 显示器操作。

## 5. 已知边界

- 当前每个 Shot 只有一个主要参考图，因此视觉简报只绑定该参考图；多参考图模型
  不在 M1B.1 范围。
- 语义观察由用户填写；没有 AI API、自动好坏评分或自动美术结论。
- M1B.2 矩形成对区域尚未开始。
- 4K 全分辨率来源遮罩会在后台执行，可能需要短暂等待，但不会在 GUI 线程计算。
- 完全无 Python 的干净 Windows 机器和真实高 DPI 显示器仍是 Windows Alpha
  前人工验收项。
