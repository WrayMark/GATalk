# GATalk 0.18 导航与视觉供应商验证记录

状态：离线候选验证完成，等待真实供应商与 Windows 显示环境人工试用
日期：2026-08-12

## 本轮范围

- 统一工作台首页入口和全局命令位置。
- 保持工作台切换前后的普通、最大化和全屏状态。
- 更新当前界面中的专业术语与说明文案。
- 为 Claude、智谱 GLM、火山方舟和腾讯混元增加视觉审阅适配。
- 更新 OpenAI、SiliconFlow 等默认模型和离线契约测试。
- 同步简中、繁中、英语、日语和法语的本轮界面术语。

## 自动化验证

- 完整离线测试：291 项通过。
- Windows `onedir`：构建通过；目录总计 270,022,962 字节（257.5 MiB）。
- 打包内启动烟测：`GATalk.exe --smoke-test` 退出码 0。
- 使用手册：已生成 Word 文档并渲染为 19 页检查；未发现截断、重叠或缺字。
- 供应商验证：新增接口均有 Mock、清单解析和请求契约测试；未使用真实 API Key，
  未产生联网费用。

## 供应商完成度

- 阿里云百炼、SiliconFlow、智谱 GLM、火山方舟、腾讯混元、OpenAI 与 xAI：
  视觉审阅接口已接入统一能力层并通过离线请求契约测试。
- Anthropic Claude：原生 Messages 与 JSON Schema 已接入；结构化参数被服务拒绝时，
  只在同一供应商内执行一次提示约束回退。
- Google Gemini：沿用已有适配与有界备用模型链。
- 图片生成：继续只显示具备图片生成能力的万相、Gemini / Nano Banana、
  OpenAI GPT Image 与 Grok Imagine；纯视觉审阅供应商不会错误出现在图片生成位置。
- 未声明 DeepSeek 视觉能力：截至本轮核对，没有把未确认的视觉输入接口写入产品目录。

## 人工验证保留项

1. 在最大化、全屏和普通窗口状态下，在首页与每个工作台之间往返。
2. 确认每个工作台左上方只有一个“工作台首页”按钮。
3. 在五种界面语言中检查全局导航、制作任务与验收中心和审阅器名称。
4. 使用各供应商真实账户分别执行一次场景视觉审阅；确认模型权限、地域、额度和
   结构化返回。真实调用由用户主动触发，不阻塞离线候选。
5. 在 Windows 125% 和 150% 缩放下检查工具栏截断与菜单布局。

## 已知限制

- 模型权限、地区可用性、额度、计费与服务端 Schema 支持以各供应商账户为准，离线
  契约测试不能代替真实联网验证。
- 繁中、英语、日语和法语已同步本轮关键导航与审阅术语，但完整商业发行仍需母语
  审校和多语言截断检查。
