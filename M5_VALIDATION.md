# M5 作品研究验证记录

版本：0.6.0a0

日期：2026-07-30

状态：内部候选，等待真实作品人工试用

## 本轮范围

- 新增应用工作台首页。
- 保留“场景美术控制”，新增独立“作品研究”模块。
- 单图研究包保存标题、类型、研究目标、已知背景、个人笔记、原图哈希、本地
  证据、AI 结果和观察状态。
- 导入保留原始字节；工作副本继续执行 EXIF 方向与 ICC 到 sRGB 流程。
- 新增本地单图明度、色彩、九宫格空间、边缘密度和注意力代理。
- 新增 CG 主美十二维严格 JSON 审阅、跨维度因果链、具体场景拆解、优点、
  局限、画面标注、可迁移规律和观察问题。
- AI 继续使用现有 Provider、Windows 系统凭据、发送清单、后台取消/重试和
  错误脱敏。

## 自动验证

- 单图分析固定输入确定性。
- 三阶明度比例、九宫格归一化坐标和注意力代理范围。
- 原始 NumPy 图片不被分析修改。
- 中文与空格路径的原始字节、SHA-256、保存与恢复。
- 作品研究严格 Schema 和离线 Mock 十二维契约。
- 工作区注册和应用首页双模块入口。
- Qt 窗口导入、后台分析、本地结果、目标与笔记恢复。
- M0.5 至 M4 全量回归。

当前结果：

- 完整离线自动化测试：177 项通过。
- 源码 `--smoke-test`：退出码 0；覆盖本地算法、研究存储、十二维 Mock、
  工作台首页和作品研究窗口创建。
- Windows `onedir`：`dist-m5-artwork-study/SceneLens/`。
- 候选目录：228 个文件，266,644,372 bytes，约 254.3 MiB。
- 打包内 `--smoke-test`：退出码 0。
- 1540 × 920 离屏界面截图已检查；研究设置、单图画布、本地证据和滚动面板
  没有重叠或裁切。
- `SceneLens_使用手册.docx` 已重新生成并复制到候选目录；结构检查为 122 段、
  23 个标题、1 节、无替换字符，DOCX ZIP 完整。
- 标准 DOCX 渲染器因本机未安装 LibreOffice 无法生成逐页 PNG，因此 Word
  手册的逐页视觉 QA 仍未完成。

## 真实作品试用步骤

1. 启动 SceneLens，在首页选择“作品研究”。
2. 新建一个作品研究，输入标题并选择保存目录。
3. 导入一张原画、环境概念图、Color Key 或优秀场景截图。
4. 填写作品类型、本次研究目标和已知背景；不知道的背景留空。
5. 在“本地证据”查看明度直方图、Oklab 色板、空间网格和注意力代理。
6. 点击色板颜色，检查来源遮罩；按 `Esc` 退出。
7. 在顶部切换灰度、三阶/五阶、剪影、缩略图和构图辅助。
8. 在“专家拆解”选择真实视觉 Provider，核对发送清单后主动发送。
9. 检查十二个维度是否都有具体画面证据、解释、效果、评价、学习点和
   不确定性。
10. 检查跨维度因果链、具体场景内容、画面标注和可迁移规律是否有教学价值。
11. 在“学习笔记”写下自己的判断，保存并关闭。
12. 重新打开 `.scenelens-study` 目录，确认图片、状态、测量、AI 结果和笔记
    全部恢复。

## 人工观察重点

- 是否真正解释“为什么有效”，而不是把十二项写成长篇套话。
- 抽象分析是否落回具体位置，具体场景内容是否解释视觉或叙事作用。
- 是否能区分画面事实、测量、专家推断和背景假设。
- 是否承认单图无法证明的作者意图、制作方法和技术参数。
- “注意力代理”是否被正确理解为启发式位置线索，而不是眼动或显著性真相。

## 已知未验证项

- 真实 Provider 的深度、地区、配额、计费和当前模型遵循 Schema 的能力。
- 真实高 DPI 显示器和完全无 Python 干净 Windows 机器。
- 多进程同时编辑同一个 `.scenelens-study` 的只读协商；本候选应避免这样使用。
