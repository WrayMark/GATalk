# GATalk 简明使用手册

适用版本：`0.17.0`（全局语言系统）
更新日期：2026-08-12

本手册只说明当前可用功能和操作。以后每次用户可见更新都同步修订。

## 1. 启动

- 试用版：打开本次候选目录中的 `GATalk/GATalk.exe`，不要继续使用旧候选。
- 开发版：双击 `start_dev.cmd`。
- 原始图片始终只读；GATalk 不会自动联网或上传图片。

## 2. 工作台首页

- “场景美术控制”：参考图与自己的 UE 截图对比、审阅、任务和版本复查。
- “作品研究”：分析一张原画、概念图、Color Key 或优秀场景作品。
- “资产拆分工作台”：把复杂场景原画整理为资产清单、区域、生成图和展示板。
- “作品研究集合与对照研究”：把 2 至 6 件作品放在同一问题下并置研究。
- “参考资料与知识库”位于专业工作台之上，统一管理可供多个模块使用的资料。
- “审阅任务与质量门禁中心”位于平台层，统一管理跨项目任务和版本验收。
- 每个专业工作台顶部都有固定的“工作台首页”大按钮，旁边是
  全局检索、任务中心和全局设置。模块自己的导入和分析工具在下一行。

### 切换界面语言

1. 打开“设置 > 全局设置”，或按 `Ctrl+,`。
2. 在“界面语言”选择语言。
3. 点击“应用”或“确定”。界面立即切换，以后启动继续使用该语言。
4. 选择“跟随系统”时，GATalk 按 Windows 显示语言选择最接近的受支持语言。

简体中文是完整基准语言。繁体中文、英语、日语和法语
当前标为“预览”；设置页会显示固定界面文案覆盖率和审校状态。没有译文的内容
回退为简体中文，不会显示空白。

语言切换只作用于软件界面和之后新发起的 AI 输出要求。项目名、用户笔记、导入
资料和已有 AI 历史不会被自动翻译或改写。远程 AI 是否严格遵循语言要求仍取决于
供应商；保存的结果保留生成时原文。

### 全局检索

1. 在首页或任一工作区按 `Ctrl+K`。
2. 输入项目、Shot、资料、作品研究、资产、生产规格、任务或门禁名称。
3. 双击结果回到来源工作区和具体对象。

检索范围是已经在 GATalk 打开过的本地工作区。它不会扫描整块磁盘、访问网页
或上传索引。项目移动后先用“项目诊断与恢复”重定位。

## 3. 参考资料与知识库

### 最短流程

1. 在首页进入“参考资料与知识库”，新建或打开资料库目录。
2. 在左侧新建集合，例如“雾景”“建筑语言”或“灯光案例”。
3. 导入原画或文档，添加 ArtStation、文章、教程链接，或新建项目笔记。
4. 在右侧填写来源类型、作者、项目、标签、资料说明和研究笔记，保存。
5. 图片需要局部研究时点击“建立局部截图”，在原图上拖出范围并确认。
6. 需要翻译时粘贴原文，选择供应商，查看发送内容后主动确认；核对译文再保存。
7. 点击“引用到项目”，选择场景审阅、作品研究、作品对照或资产拆分项目目录。
8. 用搜索框按标题、作者、项目、标签、笔记、原文和译文筛选。
9. 按 `Shift` 或 `Ctrl` 多选 2 至 6 张图片，点击“建立对照研究”。
10. 点击“打开视觉资料板”，把所选资料放到可编排画布中。

- 同一资料可以属于多个集合，资料库只保存一个条目和一份导入文件。
- 网页链接默认标记“尚未核实”；软件不会自动访问或下载网页。
- ArtStation 只保存用户输入的来源链接，不绕过站点权限抓取作品或正文。
- 局部截图保留父资料和归一化位置，写入资料库的派生目录；原图字节不改变。
- AI 翻译只发送确认窗口列出的原文，不发送图片、本地路径或其他资料条目。
- 译文保存供应商和模型；离线 Mock 只验证流程，不提供正式翻译。
- 跨项目引用是资料到项目的索引，不会把资料库修改静默写回项目。
- 美术参考资料域已启用。关卡设计和策划资料域已预留，但本版没有伪造空功能。
- 从资料库建立对照研究时会复制所选图片字节，并保留资料库来源引用；两边后续独立保存。

### 视觉资料板

1. 选择资料后打开视觉资料板；也可以先打开空白资料板。
2. 添加资料卡或文字卡并拖动排列；按 `Shift` 多选，拖框可批量选择。
3. 选择两张卡片后建立连线；需要整齐布局时使用“网格排列”。
4. 关键阶段建立快照。误改后选择快照恢复。
5. 使用“导出 PNG”生成分享图；结构化资料板仍随资料库保存。

`Ctrl+鼠标滚轮` 缩放，`Delete` 删除所选卡片，`Ctrl+S` 保存。删除资料库条目
会同步清理资料板中的引用卡片和失效连线，不修改原始图片。

## 4. 作品研究集合与对照研究

### 最短流程

1. 从资料库选择图片建立研究，或在首页直接进入并新建项目。
2. 输入核心研究问题和已核实背景。
3. 勾选 2 至 6 件作品，并选择构图、层级、明度、色彩、灯光、空间等比较维度。
4. 点击“运行本地对照”，查看双方明度、黑白灰、饱和度、中性色和细节密度差异。
5. 在“专家对照”选择 AI 供应商，查看发送清单并主动确认。
6. 阅读逐件策略、逐轴共同点与差异、跨维度关系、可迁移规律和研究边界。
7. 在“研究结论”写下自己的综合判断并保存。

- 第一件勾选作品是本地差异表的基准；选中作品后点击“设为基准”即可更换。
- 本地数值只描述画面特征，不自动判断优劣。
- 离线 Mock 只验证请求、保存和显示流程，不读取图像语义。
- AI 结果必须使用简体中文，并区分画面证据、测量与解释。
- 原图副本、SHA-256、本地分析、AI 结果和人工笔记会随项目恢复。

## 5. 审阅任务与质量门禁中心

### 从专业工作台建立任务

1. 在场景美术控制中确认审阅发现或优化预演任务；在作品研究中选中一个研究
   维度；在资产拆分中选择一项或多项资产。
2. 点击“加入审阅中心”。只有用户主动选择的内容会进入中心。
3. 返回工作台首页，进入“审阅任务与质量门禁中心”。
4. 检查来源项目、来源对象和来源版本，补充处理说明和可验证的验收条件。
5. 设置优先级和状态并保存。
6. 需要时指定制作阶段、截止日期和前置任务。

重复加入同一个来源对象时不会建立第二条任务。审阅中心不复制原图，也不会
反向修改来源项目；删除中心任务不会删除专业项目中的分析或资产。

### 按版本复查任务

1. 选择任务，点击“记录新版本复查”。
2. 填写版本名称和可选版本 ID。
3. 选择“已改善、无明显变化、进一步偏离、已解决”或“证据不足”。
4. 写明本次判断使用的画面或测量证据并保存。
5. 每次复查都会追加历史记录，不覆盖上一版本。

### 任务依赖与批量操作

- 在任务详情中选择前置任务。前置任务未完成时，该任务显示阻塞来源。
- GATalk 会拒绝自身依赖、已删除任务和循环依赖。
- 在任务列表中按 `Shift` 或 `Ctrl` 多选，可批量设为处理中或已完成。

### 设置质量门禁

1. 打开“质量门禁”页，点击“新建质量门禁”。
2. 填写门禁名称、维度、所属项目和明确的验收条件。
3. 决定它是否为必需门禁并保存。
4. 新版本完成后点击“评估当前版本”，记录通过、需复核、未通过或证据不足。
5. 需要快速建立基线时，选择制作阶段和项目，点击“应用阶段门禁模板”。

质量门禁由用户定义。GATalk 不用综合分数代替判断，也不会把 AI 结论自动标记
为人工验收通过。

## 6. 作品研究

### 最短流程

1. 在首页选择“作品研究”。
2. 点击“新建作品研究”，选择目录并输入标题。
3. 导入一张图片。
4. 填写作品类型、本次想研究什么和已知背景。
5. 在“本地证据”查看明度、色板、空间网格和注意力代理。
6. 在“专家拆解”选择视觉 AI，查看发送清单后主动确认。
7. 阅读十二维拆解和跨维度因果链。
8. 在“学习笔记”写下自己的判断，保存。
9. 需要继续规划制作资产时，点击“交给资产拆分”，选择新项目保存位置。

交接会复制原图字节，并保存研究目标、笔记和有限的研究摘要快照。资产拆分中的
修改不会反向改写原作品研究；发送给 AI 时不会包含本地项目路径。

### 本地观察

- 支持原图、灰度、三阶/五阶明度、伪色、溢出警告、剪影、缩略图、明度模糊
  和灯光明度代理图。
- 构图辅助包括三分法、黄金分割、对角线、中心、三角形和透视线。
- 点击 Oklab 色板颜色可查看来源区域；按 `Esc` 退出。
- “注意力代理”只综合局部反差、边缘密度和彩度，不是眼动、显著性或好坏判断。

### AI 专家拆解

- 十二个维度：构图、视觉层级、明度、色彩、光、空间、形状、边缘细节、材质、
  环境叙事、风格技法和情绪。
- 每项包含观察、画面证据、测量证据、解释、观看效果、评价取舍、学习点和
  不确定性。
- 另外显示跨维度因果链、具体场景内容、画面标注、可迁移规律和继续观察问题。
- 所有面向用户的 AI 内容固定使用简体中文，内部评价状态也会转换为中文。
- 若首次返回英文或繁体中文，软件最多追加一次不含图片的中文规范化请求；
  发送确认窗口会提示可能增加费用。
- 若 Gemini 当前模型重试后仍返回 503 容量不足，软件会按发送清单改用同一
  供应商的备用模型一次；完成状态会显示实际模型。
- 旧版英文结果不会被本地硬翻译或继续展示，重新审阅即可生成中文结果。
- 离线 Mock 只验证流程，不分析图片语义，也不代表本地 AI 推理。

### 保存内容

`.scenelens-study` 目录保存原图副本、SHA-256、本地证据、AI 结果、研究目标、
已知背景、观察状态和个人笔记。原始图片不会被覆盖。

## 7. 资产拆分工作台

### 三种并列方式与共享拆分方案

- “清单与校正”：先确认场景结构和拆分方案，再生成清单并人工校正。
- “自动资产板”：按当前拆分方案生成候选资产并合成一页或多页展示板。
- “生成提示语”：生成可复制给其他生图工具的提示语；可继续提交修订要求，
  也可在软件中直接编辑。
- 三种结果互不覆盖，但可保存在同一个资产项目中。
- 三种方式共用视觉分析供应商、模型和 API Key。前两种方式另外共用图片
  供应商、图片模型和分辨率。
- 清单模型负责识别和输出结构化资产；Nano Banana、万相、GPT Image 和
  Grok Imagine 等图片模型只负责概念图与展示图生成。
- 工作区顶部持续显示“当前拆分依据”。三种方式共用当前方案，但各自输出独立
  保存。修改拆分深度后，旧自动资产板和旧提示语会标记“旧版依据”，不会删除。

### 场景理解与拆分程度

1. 导入主原画后，打开“清单与校正 → 拆分依据”。
2. 可先选择“检查发送内容并分析场景结构”，也可直接从预设新建。
3. 检查场景原型、空间结构、生产系统、资产族和不确定性。
4. 选择“关卡空间与整体”“完整建筑与装配体”“可复用生产套件”“细节组件”
   或“自定义混合层级”。
5. 分别调整建筑、道具、植被、地形、材质等类别的 0–4 级深度。
6. 填写人工约束，点击“保存方案”。
7. 进入“资产清单”，按当前方案生成清单。

深度 1 只保留场景区域或完整对象；2 拆到完整建筑、桥梁、岩组或群落；3 拆到
可复用生产套件；4 才继续拆门窗、框架、檐口、栏杆、装饰和控制变体。不同方案
的资产互不覆盖，可在同一项目中切换。自然环境会按地形、岩组、物种、变体、
群落和地被理解，不照搬建筑拆法。

### 资产拆分提示语

1. 导入主原画，可选补充参考，打开“生成提示语”。
2. 在“1 生成设置”选择供应商、模型和目标外部生图工具。
3. 点击“检查发送内容并生成初稿”，核对后确认。
4. 向下滚动到“2 当前提示语”，检查中文、英文、负面提示和硬约束。
5. 在“3 继续修订”填写要求；默认只发送文字。
6. 需要 AI 重新观察原画时，勾选“本次迭代重新附带原画”。
7. 可直接改文字并点击“保存手动修改”；每次保存都会新增历史版本。
8. 点击“复制中文”“复制英文”或“复制完整提示语”，粘贴到外部生图工具。

初稿和每次协商各消耗一次所选视觉模型调用；复制、手动编辑和保存不调用 API。
“离线 Mock”只验证流程，不会理解原画。项目重开后会恢复会话和所有版本。

### 资产生产规格与交接

1. 完成一套资产清单后打开“生产交接”页。
2. 点击“同步生产规格”，为当前方案建立规格记录。
3. 逐项核对资产编码、目标尺寸说明、枢轴、几何策略、材质槽、贴图组、LOD、
   碰撞、Nanite、UE 目录、依赖和交付物。
4. 保存修改。重复编码、缺失依赖或循环依赖必须先修正。
5. 导出 JSON 或 CSV，交给建模、材质或项目管理流程。

自动建立的规格只是规划起点，尤其不代表 AI 已从单张原画确认真实尺寸、不可见
结构、枢轴或 UE 路径。GATalk 不会直接写入 UE 工程。

### 右侧工作区与滚动

- 右侧页面按当前任务分段排列，不要求在一个屏幕内显示全部内容。
- 内容超过窗口高度时，右侧会显示纵向滚动条；鼠标放在右侧后可直接滚轮滚动。
- “拆分依据”“资产详情”“生成与导出”“自动资产板”和“生成提示语”均支持纵向滚动。
- 中央原画画布保持独立，不会随右侧页面滚动。

### 全自动资产板

1. 新建或打开资产项目，导入主原画；补充参考可不填。
2. 打开右侧“自动资产板”。
3. 选择清单供应商、图片供应商、模型、分辨率和资产数量上限。
4. 点击“检查发送内容并生成资产板”。
5. 核对图片、模型和最大调用次数后确认。
6. 完成后查看首张资产板；点击“导出当前运行”保存全部分页图片与 JSON。

自动资产板可能产生多次付费调用。遇到供应商级故障时，软件会停止后续资产，避免重复无效请求。

### Gemini 图片模型

- Nano Banana 2：`gemini-3.1-flash-image`，默认均衡选项。
- Nano Banana Pro：`gemini-3-pro-image`，适合最高质量和复杂指令。
- Nano Banana 2 Lite：`gemini-3.1-flash-lite-image`，速度和成本优先，只使用 1K。
- 三者可使用同一 Gemini API Key，但仍受该 Google 项目的模型权限、计费和配额限制。
- `0.8.1` 已修复 1K/2K/4K 在 Google 原始 REST 接口中的枚举格式错误。

### 常用快捷键

- `Ctrl+Z` 撤销资产编辑；`Ctrl+Shift+Z` 或系统重做键重做。
- 资产列表用 `Shift` 连选、`Ctrl` 增减选择、`Ctrl+A` 全选。
- 区域模式下按住 `Shift` 在画布空白处拖动，可框选多个资产区域。
- 资产列表聚焦时按 `Delete` 删除选中资产；`Esc` 退出当前工具或遮罩。

### 最短流程

1. 在首页进入“资产拆分工作台”，新建资产项目。
2. 选择场景类型，填写制作目标，导入主原画；补充参考可选。
3. 在“拆分依据”分析场景结构或选择预设，校正后保存。
4. 在“资产清单”选择视觉供应商，查看发送清单并主动确认。
5. 检查资产分类、层级、复用组、优先级和原图区域。
6. 拖动区域或在“资产详情”修改名称、分类、父级、证据和制作策略。
7. 可新增、拆分、合并或删除资产；用户修订不会被后续 AI 覆盖。
8. 在清单第一列勾选需要生成的资产。
9. 在“生成与导出”选择独立概念图、保守遮挡补全图或评审展示图。
10. 导出单项图片、分页 `asset_board_01.png` 等文件和 `asset_manifest.json`。

### 画布与遮罩

- “框选资产”后在原画拖出矩形；可移动或拉动手柄调整。
- “可见遮罩”只显示矩形内的算法近似像素，不是精确实例分割。
- 按 `Esc` 退出框选或遮罩。原始图片不会被修改。

### 信息来源

- 原画可见证据：能直接从图中核对。
- AI 推断：类别、模块边界、复用或结构判断。
- 用户补充／修订：用户确认后的内容，优先级最高。
- AI 生成补全：不可见部分的概念假设，不是原画事实。
- 离线 Mock 只验证清单、保存、遮罩、生成和导出流程，不分析图片语义。

### AI 清单结构修复

- AI 偶尔会返回不存在的父资产 ID、重复 ID 或循环父级。新版会保留资产，
  只取消或修正无法成立的引用，不要求重新消耗一次 API 调用。
- 修复发生时会显示中文摘要，并写入该次 AI 运行记录。
- GATalk 不会借此改写资产名称、分类、画面证据或制作建议。看到修复提示
  后，请在资产树中人工检查父子层级。

### 图片生成失败

- 失败项悬停可查看真实错误。
- 若显示“未发送”，表示软件识别到供应商级故障并主动停止剩余调用。
- 401/403 通常是凭据或权限；404 通常是模型不可用；429 是额度或频率限制；5xx 是服务端暂时故障。
- “没有返回图片”可能来自内容安全限制或请求未完成，窗口会显示 Gemini 的完成原因。
- 若仍看到 `response_format.image.image_size`，先确认启动的是 `0.12.0`
  候选目录中的 EXE，不要从旧目录覆盖启动。

## 8. 场景美术控制最短流程

1. 点击“新建项目”，选择保存位置并命名。
2. 在左侧打开“制作意图”，填写目标风格、时间、天气、情绪、焦点和限制。
3. 新建 Shot，例如“村口固定机位”。
4. 导入参考图，再导入当前 UE 截图。后续截图会成为同 Shot 的新 Version。
5. 在右侧查看全图测量、共享色板和三阶明度差异。
6. 需要局部比较时，在“对比分析”中创建并配对矩形区域。
7. 将确认的问题转为修改任务。
8. 完成 UE 修改后导入新 Version，再进行复查。

项目会自动保存。`Ctrl+S` 可立即保存。

## 9. 场景图片查看与基础分析

- 滚轮：缩放。
- 左键拖动：平移画面。
- 双击画布：恢复适配。
- “同步视图”：同步左右缩放和平移。
- “A/B 单图”：按 `Space` 切换参考图和当前截图。
- `Esc`：退出颜色遮罩、灯光标注、区域模式或优化预览。
- “构图辅助”：三分法、黄金分割、对角线、中心、三角形、单点透视和
  两点透视；仅用于人工观察，不会自动评价构图。

显示模式包括：

- 原图、灰度、三阶明度、五阶明度
- 曝光伪色、高光/暗部溢出
- 可调剪影、缩略图观察、明度模糊
- 灯光明度代理图

灯光明度代理图不是真正灰模，不能剥离材质和纹理。

## 10. 对比分析

### 共享色板

- 使用同一组 Oklab 颜色比较双方占比。
- 点击颜色可查看它在左右画面中的来源区域。
- 再次点击或按 `Esc` 退出遮罩。

### 双图证据概览

1. 打开“对比分析”，在最上方查看参考图和当前截图的独立色板。
2. 在“观察指标”切换相对明度、Oklab 明度、彩度、色相或饱和度。
3. 选择 16、32、64 或 128 档；取消勾选可临时隐藏任一张图的曲线。
4. 色相模式会排除低彩度像素，并显示双方中性色比例。

两条曲线使用同一范围和纵轴尺度。这是像素分布测量，不代表色彩质量评分。

### 三阶明度

- 显示暗部、中间调、亮部比例及百分点差。
- 可调整暗部和亮部阈值。
- 这里只显示测量结果，不自动判断好坏。

### 成对区域

1. 进入“区域模式”。
2. 分别在参考图和当前截图拖出矩形。
3. 选中两侧区域，建立配对并设置名称和语义。
4. 松开鼠标后自动分析。

区域可移动、缩放、删除和保存。新 Version 可“复制上一版本区域”，复制后必须
人工检查位置。

## 11. 制作意图与参考图视觉简报

- “制作意图”：记录你希望最终画面达到什么目标。
- “参考图视觉简报”：记录参考图实际呈现的视觉特征。
- 自动测量、算法推断、AI 分析和用户填写会标记不同来源。
- 用户确认或修订过的内容不会被算法或 AI 自动覆盖。

## 12. AI 审阅与灯光审片

1. 优先选择“深度主美审阅（八维）”；专项灯光问题选择“灯光专项审阅”。
2. 无 API Key 时选择“离线 Mock”验证流程。
3. 使用真实供应商前，把 Key 存入 Windows 系统凭据。
4. 点击“查看发送清单并审阅”。
5. 核对图片、分辨率和字段后，手动确认发送。
6. 查看核心问题、证据支持或冲突，再由用户确认转为任务。
7. 使用“历史记录”切换旧结果；新审阅完成后自动显示最新记录。

“第二意见”会增加一次模型调用和费用，默认关闭。

### 深度主美审阅

- 八个维度：构图、视觉引导、焦点层级、色彩、明度、灯光、材质、世界设计。
- 每个维度分开显示制作目标、参考呈现、当前效果、优点、风险和不确定性。
- 最多五个核心问题，并给出保留项、UE 执行顺序和下一版验证方法。
- “本地证据”显示测量支持、部分支持、存在冲突或无法验证。冲突不会被隐藏。
- 离线 Mock 只验证界面和 JSON 流程，不会在本地分析图片，也不代表真实审阅。

### 灯光专项审阅

- 分为曝光与明度范围、主光与填充、焦点层级、空间分离、冷暖组织、
  阴影与剪影、雾与体积效果、游戏可读性八个维度。
- 每个维度分别说明目标、参考做法、当前效果、证据、已有优点、风险和不确定性。
- 忠于参考、强化戏剧性和优先游戏可读性三套方案可查看完整策略、UE5.3
  执行顺序、风险和验证方法。
- 没有 UE 工程参数时，性能部分只给检查清单，不伪造 Lux、EV、Actor 数量或性能收益。

### AI 审阅历史

- 场景审阅按 Shot 和 Version 保存；作品研究和作品对照也会保存每次完成结果。
- 页面默认显示最新结果。选择历史记录可查看旧版，也可单独删除。
- 删除审阅记录不会删除由用户已确认的任务、证据或个人笔记。

### AI 接口失败时

- `connection_closed`：远端或中间网络设备提前断开连接，不表示 API Key
  错误。新版会自动退避重试 3 次；仍失败时检查代理、VPN 和网络稳定性。
- 自动重试可能重复提交同一请求并产生额外费用，发送确认窗口会提前提示。
- `http_400`：请求参数或模型不兼容。新版会自动处理 Gemini 深度审阅的复杂
  Schema；仍失败时先确认正在运行 `0.5.0a4`，再恢复默认模型重试。
- 若错误包含 `generation_config.response_format.text.mime_type`，说明仍在运行
  旧版；关闭旧程序后改用本次 Gemini 接口修正版。
- Gemini 首次返回的 JSON 语法损坏、被截断或结构不完整时，GATalk 最多
  自动纠错一次。确认发送窗口会提示该过程可能再次发送同一审阅副本并增加
  少量费用。
- `finish_reason=MAX_TOKENS`：AI 回答过长导致 JSON 尾部被截断，不表示
  API Key 错误。新版会压缩内容，并只在明确截断时用更高预算纠错一次。
- 错误中的 `line` 和 `column` 表示 AI 返回文本的 JSON 损坏位置，不表示 API
  Key 错误。`0.5.0a4` 会把原始返回交给同一模型压缩并修复；第二次仍损坏时
  停止，不会无限重试。
- 若 AI 引用了本次报告中不存在的问题 ID，`0.5.0a3` 会取消无效链接并继续
  显示完整报告，不追加 API 调用。界面的“结构修复”只表示链接已取消，正文
  和真实问题没有被改写。
- `http_401`：Key 错误、过期或不属于该供应商。
- `http_403`：检查模型权限、服务开通、地域和 Key 所属工作区。
- `http_404`：模型 ID 已下线或接口不存在。Gemini 会按发送清单自动尝试
  当前备用链；其他供应商请改回其默认模型。
- `http_413`：降低发送分辨率后重试。
- `http_429`：额度不足或调用过快。
- `http_503`：供应商当前模型临时过载，不表示 API Key 错误。新版先有限重试；
  Gemini 仍失败时会按发送清单依次尝试 `gemini-3.5-flash` 和
  `gemini-3.5-flash-lite`，不会再使用 `gemini-2.5-flash`。
- 发送清单会提前显示完整备用链。若发生回退，状态栏和保存记录会显示请求
  模型、实际模型及回退原因；每次实际发送都可能产生调用费用。
- 其他 `http_5xx`：供应商服务异常，稍后重试。

错误窗口只显示供应商返回的脱敏原因。不要把 API Key 截图或复制给他人。

## 13. 优化实验室

### 目标匹配画像

- 显示明度、黑白灰、色板、彩度、冷暖、区域关系等维度。
- 可修改各维度权重。
- “估计匹配度”只代表当前算法和权重，不是作品质量评分。
- 证据不足的维度会明确标出，不会自动猜测。

### 安全调色

1. 调整曝光、对比、白平衡、阴影、中间调、亮部和彩度。
2. 选择全图或当前选中的配对区域。
3. 设置参考影响强度并生成预览。
4. 使用 A/B、撤销和重做检查结果。
5. 可导出 PNG 和 JSON 配方；符合条件时可导出 `.cube`。

安全调色完全本地运行，不修改原图。区域调色或参考色迁移不能导出通用
`.cube`。

### AI 优化预演

1. 选择只改灯光、只改色彩或只改雾与氛围。
2. 设置改动预算和构图、几何、资产身份保护项。
3. 查看发送清单并主动确认。
4. 检查结构漂移、构图偏移和保护区变化。

AI 输出只保存为 `AIConceptPreview`，不会成为真实 UE Version。出现“仅适合
概念参考”时，不应把预演当作可直接复现的场景结果。

## 14. 全局工具与文件安全

### 项目诊断与恢复

1. 在首页打开“项目诊断与恢复”。
2. 选择或扫描最近项目，查看入口、数据库、目录、资料引用、产物和写入状态。
3. 基础目录缺失时使用“修复项目目录”；该操作不会伪造数据库或原始资料。
4. 重大修改前点击“建立恢复点”。需要回退时选择恢复点并确认恢复。
5. 项目被移动后使用“重定位移动项目”，指向新的入口文件。
6. 需要提交问题时导出诊断报告；报告不包含 API Key、图片字节和项目正文。

恢复前 GATalk 会再建立一个安全点。恢复测试应先复制真实项目，不要把唯一原件
作为试验对象。

- `Ctrl+K` 打开全局检索；`Ctrl+Shift+P` 打开命令面板；`Ctrl+,` 打开全局设置。
- `Ctrl+Shift+J` 打开任务与供应商状态，查看 AI 调用、重试和失败原因。
- `Ctrl+Shift+D` 打开项目诊断，检查入口文件、数据库、assets 和备份数量。
- 上次异常退出后，未完成任务会标记“上次退出时中断”，首页诊断会给出提示。
- 诊断报告不包含图片、API Key、AI 提示语或项目正文；会包含用户主动检查的本地路径。

### 文件与安全

- `assets`：导入原图，保留原始字节和 SHA-256。
- `artifacts`：可重建分析结果和 AIConceptPreview。
- `exports`：审阅包、预览和配方。
- API Key 不写入项目、SQLite、JSON、日志或 Git。
- 同一项目只能由一个进程写入；第二个进程可只读打开。
- 作品研究的 `assets` 同样保留导入图片原始字节；`study.json` 保存研究状态。
- 资产拆分项目使用 `.scenelens-assets` 目录；生成图、遮罩和导出记录与原图
  分开保存。
- 资料库使用 `library.json`、`assets`、`documents`、`exports` 和 `backups`。
- 对照研究使用 `comparison.json`、`assets`、`artifacts`、`exports` 和 `backups`。

## 15. 当前限制

- 只支持静态图片和矩形区域。
- 不支持视频、HDR/EXR、多边形、自动分割或 UE 工程扫描。
- 真实 AI 供应商需要单独验证账号、地区、模型和费用。
- AI 预演必须回到 UE 实施，并用新的真实截图 Version 正式复查。
- 对照研究支持 2 至 6 张静态图片；暂不提供自由画布、自动网页抓取、图像相似搜索或本地大型视觉模型。
- 关卡设计和策划资料域只完成可注册的数据边界，尚未开发对应资料工具。
- 资产拆分不捆绑本地视觉大模型、SAM 2 或 CUDA，也不生成生产可用三维模型。
- AI 独立资产图和资产板是概念规划图，不是从原画提取出的真实三维模型；背面、
  内部结构、尺度、枢轴、拓扑和精确拼接规格仍需制作人员确认。
- AI 资产分类、遮挡补全和生成图仍需用户人工校正。
