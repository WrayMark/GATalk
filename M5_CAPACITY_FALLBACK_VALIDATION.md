# M5 Gemini 容量回退修复验证

版本：0.6.0a3

日期：2026-07-30

候选标签：`v0.6.0a3-m5-fallback`

## 问题判断

截图中的 `HTTP 503 / UNAVAILABLE / high demand` 是 Google Gemini 当前模型
临时容量不足。请求已经到达供应商，不能据此判断 API Key 错误。

## 修复

- 主模型继续有限重试 3 次。
- 只有重试耗尽的 503 才触发同供应商备用模型。
- Google Gemini 视觉审阅备用模型为稳定 `gemini-2.5-flash`。
- 备用模型只尝试 1 次，不跨供应商，不处理 400、401、403 或 429。
- 发送确认窗口提前展示备用模型和潜在额外费用。
- 完成状态和保存记录显示请求模型、尝试模型、实际模型与回退原因。
- 作品研究与场景 AI 专项审阅使用同一机制。

## 自动验证

- 容量回退、非 503 不回退、Manifest 配置和回退记录测试通过。
- 完整自动化测试：190 项通过。
- 源码 `--smoke-test`：退出码 0。
- Windows onedir：构建成功。
- 打包版 `--smoke-test`：退出码 0。
- 最终 onedir 文件数（含手册与验证记录）：228。
- 最终 onedir 大小：266,653,949 字节，约 254.3 MiB。

候选程序：

`dist-m5-fallback-a3/SceneLens/SceneLens.exe`

## 手册验证

- `SceneLens_使用手册.docx` 已更新至 0.6.0a3。
- Word 包结构检查通过，共 130 个段落、1 个 section、17 个内部文件。
- 已确认手册包含 503 容量不足、备用模型、费用和实际模型记录说明。
- 当前机器没有 LibreOffice，无法执行逐页 PNG 渲染检查。

## 真实项目试用

1. 关闭旧版 SceneLens。
2. 启动本候选目录中的 `SceneLens.exe`。
3. 进入原作品研究，保持模型为 `gemini-3.5-flash`。
4. 查看发送清单，确认其中显示备用模型 `gemini-2.5-flash`。
5. 主动开始专家拆解。
6. 若主模型仍繁忙，等待软件自动回退；不要连续点击发送。
7. 成功后检查状态栏显示实际使用模型，报告保持简体中文。
8. 若主模型和备用模型都不可用，稍后再试；这属于供应商容量问题。

真实联网结果取决于供应商当时容量与账号模型权限，不进入默认离线自动测试。
