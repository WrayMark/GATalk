# SceneLens M3 验证记录

状态：内部候选通过，等待第二个重大外部试用点  
日期：2026-07-19  
候选标签：`v0.4.0a0-m3`

## 已确认事实

- M1B.2 与 M2 回归功能继续通过。
- 完整自动化测试共 145 项，全部通过；默认测试没有发起网络请求，也未消耗
  API 额度。
- 源码 `--smoke-test` 退出码为 0。
- PyInstaller `onedir` 构建成功；打包内 `SceneLens.exe --smoke-test`
  退出码为 0。
- 包内烟测实际覆盖 M3 匹配画像、安全调色、结构漂移、图像编辑 Mock、
  `AIConceptPreview` 持久化以及真实 Version 数量不变。
- 候选目录总大小为 266,472,228 bytes（约 254.1 MiB），共 222 个文件。
- 候选 EXE SHA-256：
  `AFE1F5AB9A2168D6E7EA9B77526AD226B5402AA3D55A07C245A06C9512783656`。
- 候选程序入口：`dist/SceneLens/SceneLens.exe`。

## M3 能力覆盖

- 十维目标匹配画像、透明权重、估计匹配度和证据覆盖率。
- 曝光、对比、白平衡、阴影/中间调/亮部、彩度和有限参考色迁移。
- 全图或当前选中配对区域的安全调色；原图只读，强度使用线性 sRGB 实际插值。
- 5% 强度步进、快捷值、A/B、配方撤销重做、PNG/JSON 和受约束 `.cube` 导出。
- 万相、Gemini/Nano Banana、OpenAI GPT Image 和 Grok Imagine 的图像编辑
  请求适配器，以及完全离线 Mock。
- 结构化改图指令、保护约束和发送清单；没有用户主动确认不会调用 Provider。
- `AIConceptPreview` 与真实 Version 隔离；保存来源、输入哈希、指令、约束和
  漂移校验。
- 结构漂移、稳定/保护区变化、构图偏移和色调目标改善；超界时标记
  “仅适合概念参考”。
- 预演经用户确认后可生成任务，任务明确要求新的真实 UE Version 才能正式复查。

## 自动测试重点

- 权重改变、缺失语义证据和证据覆盖率。
- 安全调色确定性、线性插值、区域范围、原数组只读和预览阶梯。
- 配方撤销重做、PNG/JSON、LUT 可导出条件。
- AI 改动预算的 5% 步进和四档语义。
- 图像供应商的请求构造、主动发送门禁、Mock、取消、重试和错误脱敏。
- 结构漂移、保护区变化、构图偏移和目标改善的合成图片测试。
- Mock 端到端：显式确认发送、后台生成、artifact 保存、漂移校验、项目重开
  可读取、真实 Version 数量不变、用户确认后转任务。

## Provider 完成度

| Provider | 当前完成度 | 真实联网验证 |
|---|---|---|
| 离线 Mock | 完整可执行；回传当前截图验证隔离、保存和任务流程 | 不需要 |
| 阿里云万相 | Manifest、请求构造、响应解析和录制传输契约完成 | 未验证真实 Key |
| Gemini / Nano Banana | Manifest、`generateContent` 请求与内嵌图片解析完成 | 未验证真实 Key |
| OpenAI GPT Image | Multipart 请求与内嵌 base64 响应解析完成 | 未验证真实 Key |
| Grok Imagine | Multipart 适配位置和契约完成 | 未验证真实 Key |
| M2 五类视觉审阅 Provider | 继续保持 M2 完成度 | 未验证真实 Key |

“请求契约完成”不等于供应商线上接口已验证。模型 ID、账号区域、能力开通、
响应字段和计费可能变化，必须使用真实账号逐项人工烟测。

## 第二个重大外部试用步骤

1. 打开中世纪村庄项目，选择包含参考图、真实 UE 当前截图和成对区域的 Shot。
2. 进入“优化实验室”，检查十个匹配维度、证据来源和证据覆盖；修改三项权重，
   确认只改变汇总视角。
3. 在安全调色中依次轻调曝光、白平衡、阴影和彩度，以 5%、15%、25%、50%
   比较参考影响；使用 A/B 和配方撤销重做。
4. 选中一个当前成对区域，切换区域作用范围；确认区域外不变且原图文件哈希不变。
5. 导出 PNG 和 JSON 配方；关闭参考色迁移且使用全图配方时导出 `.cube`。
6. 选择离线 Mock，检查发送清单并生成 AI 预演；确认结果明确标为
   `AIConceptPreview`，项目的真实 Version 数量不变。
7. 如团队政策允许且有真实 Key，分别试用万相和一个海外 Provider；记录模型、
   地区、耗时、费用、响应格式和失败信息。
8. 对生成结果检查结构漂移、保护区变化、构图偏移和“仅适合概念参考”警告。
9. 将预演转成任务，在 UE 中实际修改后导入新的真实截图 Version，再使用 M2
   审阅与区域证据做正式复查。
10. 保存关闭并重开项目，确认 AIConceptPreview artifact、AIRun 和任务仍可追溯，
    且预演没有出现在 Version 导航中。

## 已知限制与未验证项

- 真实图像供应商尚未使用 Key 联网验证；URL-only 图片响应不会自动下载。
- 同步 HTTP 正在等待 socket 时，取消只能在返回后丢弃结果，不能保证停止远端
  计费。
- 目标匹配中的视觉焦点、灯光氛围和空间层次尚无用户/AI 语义证据时不计算。
- 安全调色不是完整调色软件；不包含曲线、二级选色、蒙版羽化或 ACES/HDR。
- 结构漂移是本地启发式代理，不等同于几何真值；AI 预演始终需要人工检查。
- 真实 125%/150% 高 DPI 显示器和完全无 Python 的干净 Windows 机器仍是
  Windows Alpha 前验收项。
