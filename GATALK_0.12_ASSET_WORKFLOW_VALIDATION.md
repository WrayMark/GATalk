# GATalk 0.12 资产工作流验证记录

## 交付范围

- 三种资产工作方式共享当前场景理解与拆分方案。
- 自动资产板记录方案 ID、方案指纹、方案名称和场景结构来源。
- 生成提示语请求包含作品研究交接、场景理解与逐类别拆分深度。
- 自动资产板与提示语在方案变化后显示“旧版依据”，历史结果继续保留。
- 资产工作台增加固定运行依据栏，重整顶层工作方式、内部页面和主要动作名称。
- 项目格式从 v4 迁移到 v5；迁移前继续自动备份。

## 自动化验证

- 完整测试：230 项通过，耗时 18.39 秒。
- 方案指纹对时间戳与确认状态稳定，对生产相关字段变化敏感。
- 提示语请求包含类别深度、场景结构和作品研究交接，且不发送本地路径。
- 自动资产板与提示语版本保存并恢复方案来源。
- 同一方案下不显示过期；修改类别深度后两种历史输出均标记旧版依据。
- 旧项目迁移后格式版本和模块 schema 为 5，并保留迁移备份。

## 人工试用重点

1. 打开同一原画，建立“完整装配体”和“生产套件”两套方案。
2. 在顶部“当前拆分依据”切换方案，确认三个工作方式同步。
3. 分别生成一份清单、自动资产板和提示语初稿。
4. 修改当前方案的建筑拆分深度并保存。
5. 检查旧自动资产板和旧提示语显示“旧版依据”，文件与历史版本仍可查看。
6. 重新生成，确认新结果绑定修改后的方案。
7. 分别切换深色、浅色和跟随系统主题，检查 125% 与 150% 缩放下的按钮、
   标签、滚动区和画布比例。

## 仍需真实服务验证

- Gemini、百炼、SiliconFlow、OpenAI、Grok 等真实供应商对新增方案上下文的
  遵循程度。
- 自动资产板的实际调用费用、限流表现和复杂场景分页质量。
- 高 DPI 实机和无 Python 干净机启动仍保留为 Windows Alpha 验收项。

## 打包与文档验证

- PyInstaller `onedir` 构建成功：`dist/GATalk/GATalk.exe`。
- 打包目录 231 个文件，共 256.58 MiB。
- 内部启动烟测持续 6 秒，进程正常存活后主动关闭。
- 深色主题资产工作台截图已检查；修正顶部方案说明挤压画布的问题。
- `GATalk_使用手册.docx` 共 10 页，已逐页渲染检查，无文字截断、重叠或空白页。
