# M4 验证记录

版本：0.5.0a4
日期：2026-07-28
状态：损坏 JSON 自动恢复候选，等待真实项目人工试用

## 自动验证

- 八维 Schema 完整性、维度唯一性和引用关系
- 深度审阅离线 Mock 契约
- 有界本地证据摘要的确定性和方向性
- AI 可测结论冲突与可信度降级
- 各视觉 Provider 的图片角色标记和输出预算映射
- 七类构图辅助线归一化绘制
- schema v7 保存恢复与迁移前备份
- M0.5 至 M3 全量回归

结果：

- 完整离线自动化测试：169 项通过。
- 源码 `--smoke-test`：退出码 0。
- Windows `onedir`：`dist-m4-jsonrepair-final/SceneLens/`。
- 打包内 `--smoke-test`：退出码 0。
- 候选目录：224 个文件，266,563,109 bytes，约 254.2 MiB。
- `SceneLens_使用手册.docx` 已重新生成并放入候选目录。
- DOCX 结构检查通过：99 段、1 节、无替换字符。标准渲染器因本机未安装
  LibreOffice 无法生成页面图，因此本轮未完成逐页图像检查。

## 真实项目试用

1. 打开包含制作意图、参考图、当前截图和成对区域的项目。
2. 在“AI 审阅与任务”选择“深度主美审阅（八维）”。
3. 选择真实视觉 Provider，确认模型 ID 和发送分辨率。
4. 点击“查看发送清单并审阅”，核对两张图片和字段后发送。
5. 检查八个维度是否都区分制作目标、参考呈现和当前效果。
6. 检查核心问题是否不超过五个，且包含证据、影响、动作和下一版验证。
7. 检查“本地证据”列；若出现冲突，确认结论仍显示且原因可见。
8. 检查执行顺序、需要保留和可信度限制。
9. 选择一个问题转为任务，关闭并重开项目确认结果仍在。
10. 在顶部切换七类构图辅助，检查左右画布、缩放、重开恢复和 `Esc` 关闭。

## 人工观察重点

- 审阅深度是否明显高于旧“主美专项审阅”
- 制作阶段是否影响批评标准
- 参考图与当前截图身份是否被模型正确理解
- 材质和叙事维度是否承认截图证据限制
- UE 执行顺序是否具体但没有虚构工程参数

真实 Provider 的模型能力、配额、地区和服务响应不进入默认自动测试。

## Gemini 深度审阅接口修复

- 真实调用确认 Key 和模型可到达服务，但 M4 八维 Schema 被 Gemini 以
  `HTTP 400 / INVALID_ARGUMENT` 拒绝。
- Google 官方说明复杂或深层 Schema 可能被拒绝。`0.5.0a1` 曾自动发送
  顶层紧凑约束，并在提示中保留完整 Schema，返回后继续执行完整本地校验。
- 若 Gemini 仍拒绝带 Schema 的请求，同一次用户操作会自动切换为 JSON
  兼容模式重试一次；不会无限重试。
- 百炼和 SiliconFlow 的 JSON 对象模式现在也会收到完整 Schema 文本约束。
- 新增复杂度、紧凑约束、自动兼容重试和 JSON 模式完整 Schema 契约测试。
- 接口修复后完整离线回归 160 项通过，源码和打包烟测退出码均为 0。

## Gemini 返回结构纠错

- `0.5.0a1` 真实调用已越过 HTTP 400，但模型漏掉
  `target_readback` 必填字段，并把 `evidence_claims` 对象数组输出为字符串
  数组。
- `0.5.0a2` 的 Gemini 服务端约束保留全部嵌套字段、必填字段和数组元素类型，
  只移除容易提高语法复杂度的枚举、数值范围等限制。
- 完整 Schema 继续随请求发送，并增加一个本地可验证的 JSON 结构模板。
  `evidence_claims` 缺少完整矩形、指标和阈值时必须返回空数组。
- 首次结果仍不符合 Schema 时，同一次用户操作最多自动执行一次结构纠错。
  纠错仍使用完整本地 Schema，失败后停止并显示明确字段位置。
- 确认发送窗口明确提示可能再次发送同一审阅副本并增加少量费用。
- 新增截图同类错误、嵌套结构、一次纠错上限和费用提示回归测试；完整离线
  自动化测试 164 项通过，源码与打包内烟测退出码均为 0。

## Finding 引用完整性

- 真实调用中，Gemini 偶发在 `dimension_reviews.linked_finding_ids` 中引用
  本次 `findings` 列表并不存在的 ID。该结果已经通过 JSON 结构校验，但没有
  通过跨字段引用完整性校验。
- `0.5.0a3` 会在本地移除维度和执行动作中的悬空问题链接，不追加网络请求，
  不修改审阅正文、真实 finding 或动作内容，也不根据相似名称猜测对应关系。
- 若发生清理，界面在本地校验区显示“结构修复”及被取消的 ID；保存的 AI
  结果只保留有效引用。
- 深度审阅提示同步要求所有问题链接只能使用本次真实存在的 `finding_id`。
- 截图同类多维度悬空引用、动作悬空引用、有效链接保留和界面提示均有测试；
  完整离线自动化测试 166 项通过，源码与打包内烟测退出码均为 0。

## Gemini 损坏 JSON 自动恢复

- 真实调用偶发返回无法解析的 JSON，错误位置为 `line=461,column=17`。请求已
  到达模型，失败点是响应文本语法损坏或截断，不是 API Key。
- `0.5.0a4` 在解析前保留原始返回文本。若 JSON 语法无效，同一次用户操作
  最多调用一次同一模型，携带原文、解析位置、完成原因和完整 Schema，要求
  精简措辞并输出完整 JSON。
- Gemini 把文本拆成多个响应片段时，SceneLens 会先按顺序完整拼接，再判断
  JSON 是否损坏，避免把正常的分段响应误判为截断。
- 纠错不得增加美术结论或虚构坐标；第二次仍不是有效 JSON 时停止并显示新的
  解析位置，不执行宽松本地猜测，也不无限重试。
- 发送确认窗口明确覆盖语法损坏、模型截断和结构不完整三种额外调用原因。
- 截图中的 `line=461,column=17`、`MAX_TOKENS`、多文本片段、一次修复成功和
  第二次失败停止均有离线测试；完整回归 169 项通过，源码与打包烟测退出码
  均为 0。
