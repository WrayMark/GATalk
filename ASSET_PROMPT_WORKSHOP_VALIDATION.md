# 资产拆分提示语验证记录

候选版本：0.9.0
日期：2026-07-31

## 已完成

- 在资产拆分工作台增加第三条独立流程“资产拆分提示语”。
- 初稿读取主原画、最多三张补充参考、场景类型、制作目标和备注。
- 输出中文提示语、英文提示语、负面提示、硬约束、图片理解和资产组依据。
- 支持多轮协商；默认只发送文字，可由用户主动选择重新附带原画。
- 支持直接编辑、保存为新版本、查看历史版本和复制提示语。
- 保存会话、协商记录、AI/手动修订、目标工具、Provider、模型和输入哈希。
- 资产项目格式升级到 v3；旧项目迁移前自动备份，更换主原画后旧会话失效。
- 原图保持只读；所有联网调用仍需查看发送清单并主动确认。

## 自动验证

- 新增功能相关测试：21 项通过。
- 完整自动化套件：221 项通过。
- 覆盖严格 JSON Schema、初稿附图、后续纯文字修订、Mock、工作区注册、
  中文和空格路径、v2 到 v3 迁移备份、会话保存恢复、手动编辑、复制和
  统一 AI 设置同步。
- 源码界面截图烟测通过，截图为
  `.qa/asset-prompt-workshop-smoke/asset-prompt-workshop-0.9.0.png`；
  可用 `scripts/render_asset_prompt_workshop_smoke.py` 重建。
- Windows `onedir` 构建成功；打包内 `--smoke-test` 退出码为 0。
- 候选包共 229 个文件、268,974,743 bytes（约 256.5 MiB）。
- 主程序 SHA-256：
  `8B6192C4C97543CB75605D812617776CE5E45F61CD2C5ACD68D1EBFCDEB9FD9F`。

## 验证边界

- 离线 Mock 只验证请求、Schema、保存和界面流程，不理解图片。
- 本轮未使用用户真实 API Key 发起付费调用。真实模型的提示语质量、账号
  权限、区域、配额、模型可用性和实际费用属于人工联网验收项。
- 后续默认不重新发送图片可以减少图片传输，但每次 AI 协商仍是一次模型调用。
- 复制到外部工具后的出图质量由外部模型、账号和参数决定，SceneLens 不自动
  提交，也不能保证第三方模型严格遵守提示语。

## 人工试用步骤

1. 启动候选包，在首页进入“资产拆分工作台”。
2. 新建或打开资产项目，导入主原画，可选导入补充参考。
3. 打开右侧“资产拆分提示语”。
4. 选择 AI 供应商、模型和目标生图工具。
5. 生成初稿前查看发送清单并确认。
6. 检查中文、英文、负面提示、硬约束和资产组依据。
7. 输入“减少次要道具、强化建筑模块化”等意见，继续让 AI 修订。
8. 确认默认修订清单不包含图片；再勾选重新附图测试一次。
9. 手动修改提示语并保存，切换历史版本确认旧内容保留。
10. 复制中文、英文和完整提示语，粘贴到文本编辑器核对。
11. 关闭并重开项目，确认会话、记录和版本全部恢复。
