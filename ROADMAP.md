# SceneLens 路线图

## M0：产品与技术收敛

状态：完成

- 明确产品定位、范围和隐私边界。
- 确认 Windows/PySide6 技术路线。
- 确认 Project → Shot → Version 数据关系。
- 完成第一轮仓库与许可证调研。

## M0.5：技术验证原型

状态：完成并冻结（2026-07-19）

范围：

- PySide6 主窗口
- 参考图与当前截图拖入
- 双图并排
- 同步缩放和平移
- A/B 切换
- 灰度、可调高斯模糊
- 三阶/五阶明度归纳
- 默认 8 色 Oklab 色板及面积比例
- 基础明度直方图
- PyInstaller `onedir`

不包含：

- SQLite、项目保存、镜头和版本系统
- 区域选择、AI 审阅、修改任务
- 正式 UI 美化

验收：

- 真实概念图与 UE 截图可完成核心交互。
- 4K 图片可导入，分析不长时间阻塞界面。
- 色板具有基本美术可读性。
- 未安装 Python 的 Windows 环境可启动 `onedir`。
- PySide6、Pillow、OpenCV Headless、NumPy、Colour Science 无明显冲突。
- ICC、EXIF 和 sRGB 流程通过自动测试与手工样例验证。
- 输出 `M0.5_VALIDATION.md`。

用户已使用真实图片完成试用，未发现影响使用的问题，并确认 M0.5 通过
人工验收。

说明：当前开发机上的 `onedir` 已通过启动烟测；完全未安装 Python 的干净
Windows 环境仍未验证，保留到 M6 发布验收，不将其误记为本轮已确认事实。

## M1A：项目、镜头与版本系统

状态：真实项目人工试用通过并冻结（2026-07-19）

- 创建、打开和保存项目。
- 最近项目列表。
- Project → Shot → Version。
- Art Brief 核心数据模型。
- 原始资源复制、哈希、SQLite、迁移与备份。
- 自动保存、保存失败保护和工作状态恢复。
- 分析产物可重建，历史版本的分析记录可恢复。

M1A 验收闭环：

1. 创建项目并填写 Art Brief；
2. 创建 Shot，导入参考图与当前截图；
3. 保存、关闭并重新打开项目；
4. 恢复当前 Shot、Version、显示模式、视图和分析参数；
5. 添加同 Shot 的新 Version，并可查看历史分析结果。

工程验证：

- schema v1、原子清单写入和迁移前备份已实现；
- 中英文及空格路径下的原始字节复制与 SHA-256 已测试；
- 创建、保存、重开、添加新版本和切回历史版本的 UI 纵向测试已通过；
- M0.5 原有 19 项测试继续通过；M1A 冻结时完整套件共 31 项；
- Windows `onedir` 已构建，内部图片分析与 SQLite 项目烟测退出码为 0；
- M1B 仍未启动。

真实高 DPI 显示器和完全无 Python 的干净 Windows 环境测试保留为 M6
Windows Alpha 前验收项。

## M1B：参考理解与证据化对比版

状态：M1B.1 真实项目试用通过并冻结，进入 M1B.2

### M1B.0：架构与数据基础

状态：完成（2026-07-19）

- M1A 冻结回退点与同项目多进程写保护。
- 只读打开与异常退出陈旧锁恢复。
- 模块化单体边界和分析器注册接口。
- 制作意图与参考图视觉简报字段来源模型。
- SQLite schema v1 → v2 迁移与迁移前备份。
- 模块配置加载的预设选项。

### M1B.1：Brief 与全图证据化比较

状态：真实项目人工试用通过并冻结（2026-07-19）

- 制作意图界面更新、Project 字段和 Shot 覆盖。
- 参考图视觉简报基础界面、保存恢复与自动测量关联。
- 共享 Oklab 色板及双方占比差。
- 独立/共享色板来源遮罩。
- 三阶明度比例比较和缩略图。
- 完成后构建 `onedir` 交付真实项目试用。

### M1B.2：成对区域

状态：真实项目人工试用通过并冻结（2026-07-19）

M1B.2A 已完成：

- 归一化矩形区域创建、命名、移动、缩放、删除和恢复。
- 参考/当前区域配对。
- schema v4、迁移前备份和 Version 区域复制。
- 区域模式、配对列表、同色编号和叠层显隐。

M1B.2B 已完成：

- 成对区域明度、Oklab、彩度、色相和共享色板组成。
- 区域分析过期、详细比较和区域内颜色遮罩。
- 完成后再次构建 `onedir` 交付试用。

候选版已完成 70 项自动化测试、4K 区域性能验证、Windows `onedir` 构建和
打包内部烟测。用户已使用真实项目完成试用并确认通过。

M1B 不自动生成好坏评分或美术结论，不接入 AI API、模型、CUDA、视频、
自由多边形、插件市场或 UE 工程扫描。

## M2：AI 主美控制台与灯光审片台

状态：内部候选完成，已建立重大外部试用点并连续进入 M3
候选版本：`v0.3.0a0-m2`

- 已完成：共享工作台 schema v5、显式 Workspace/Reviewer/Provider 注册和示例模块。
- 已完成：五类视觉 Provider 适配与四类图像编辑能力位置；凭据、主动发送门禁、后台取消/重试/脱敏。
- 已完成：主美/灯光严格 Schema、证据校验器、质量门禁、新 Version 变化状态、第二意见合并和六种本地灯光观察算法。
- 已完成：应用界面接入、AI Run/任务持久化、结构化灯光标注与候选 `onedir`。
- 验证：122 项测试、源码烟测和打包内烟测通过；真实 Key 联网验证保留为人工项。
- 建立 `VisionReviewProvider`、`StructuredOutputProvider`、
  `ImageEditProvider`、能力清单、Manifest 和注册表。
- 首批适配百炼 Qwen VL、SiliconFlow Qwen VL、OpenAI、Gemini 和 Grok；
  默认测试使用 Mock，真实联网测试默认关闭。
- API Key 只进入 Windows 系统凭据存储；项目、数据库、JSON 和日志不保存密钥。
- 建立主美审阅与灯光审阅严格 Schema、最多五个核心问题、第二意见和用户确认
  转任务。
- 建立 AI 证据校验器、分维度质量门禁和新 Version 复查状态。
- 增加曝光伪色、溢出警告、剪影、缩略图、明度模糊和灯光明度代理图。
- 增加三套灯光方案、结构化画布标注和 UE5.3 执行顺序。
- 完成后交付候选 `onedir`、验证记录和第一个重大外部试用点；除真实 Key 联网
  烟测外，其余验收不阻塞进入 M3。

## M3：参考匹配与画面优化实验室

状态：内部候选完成，建立第二个重大外部试用点
候选版本：`v0.4.0a0-m3`

- 已完成：十维目标匹配画像、透明用户权重、证据覆盖率和“估计匹配度”；
  缺少证据的语义维度不猜测、不按零分处理。
- 已完成：0%–100% 参考影响强度、5% 步进和 5/10/15/25/50/75/100 快捷值。
- 已完成：全图/选中成对区域安全调色、线性 sRGB 强度插值、A/B、配方撤销
  重做、PNG、JSON 配方和受约束的 `.cube` LUT。
- 已完成：万相、Gemini/Nano Banana、OpenAI GPT Image 与 Grok Imagine
  请求适配器和完全离线 Mock 契约；所有真实发送仍需用户主动确认。
- 已完成：从制作意图、参考图视觉简报、确认任务、成对区域和保护约束构造
  结构化改图指令，强度只表示允许的改动预算。
- 已完成：AI 结果保存为 `AIConceptPreview`，与真实 UE Version 严格隔离；
  计算结构漂移、稳定/保护区变化、构图偏移及色调目标改善。
- 已完成：高漂移结果标记“仅适合概念参考”，预演可经用户确认转成任务，
  但必须等待新的真实 UE 截图进入正式复查。
- 验证：默认离线完整回归、源码/打包烟测和 `onedir` 结果记录在
  `M3_VALIDATION.md`；真实供应商 Key 与当前服务响应仍是人工联网项。

## M4：深度主美审阅与构图诊断

状态：内部候选完成，Gemini 损坏 JSON 自动恢复后等待真实项目试用
候选版本：`v0.5.0a4-m4-jsonrepair`

- 新增严格八维深度主美审阅：构图、视觉引导、焦点层级、色彩设计、明度结构、
  灯光氛围、材质可读性、世界设计与叙事。
- 审阅同时读取制作意图、参考图视觉简报、全图测量、全部成对区域、Version
  历史、锁定目标和制作阶段，不再只依赖两张图片和简短提示词。
- 每次最多五个跨维度核心问题；分别提供画面证据、测量证据、影响、动作、
  优先级、不确定性、下一版验证方法和有依赖顺序的 UE 执行计划。
- 新增有界九宫格本地证据摘要。AI 声明的明暗、局部反差、高光、暗部、冷暖和
  参考差异由原始像素复核；冲突保留并降低可信度。
- 供应商请求为每张图片显式标记 reference/current 角色，并为深度结构化输出
  配置更大的输出预算。
- 新增三分法、黄金分割、对角线、中心、三角形、单点透视和两点透视七类构图
  辅助线；仅作人工观察，不冒充自动构图结论。
- schema v7 保存构图辅助选择；迁移前继续自动备份。
- M4 不新增本地大模型、显卡依赖、自动显著性判断或泛化总分。

## M5：作品研究

状态：内部候选完成，等待真实作品人工试用

候选版本：`v0.6.0a0-m5-artwork-study`

网络可靠性修正版：`v0.6.0a1-m5-network`

简体中文输出修正版：`v0.6.0a2-m5-zhcn`

Gemini 容量回退修正版：`v0.6.0a3-m5-fallback`

- 应用启动首页和内置工作区选择。
- 独立 `scenelens.artwork_study` 模块与 `.scenelens-study` 研究包。
- 单图只读资产、哈希、本地证据、观察状态、个人笔记和 AI 结果保存恢复。
- 本地明度、Oklab、彩度、九宫格空间、边缘密度和有明确限制的注意力代理。
- 灰度、三阶/五阶、剪影、伪色、模糊、缩略图、构图辅助与色板来源遮罩。
- CG 主美十二维严格审阅、跨维度因果链、具体场景拆解、评价取舍、画面标注、
  可迁移规律和继续观察问题。
- 保留现有 Provider、主动发送、系统凭据、后台任务、离线 Mock 和无 Key
  本地工作能力。
- 公开研究方法与版权边界记录在
  `docs/ARTWORK_STUDY_RESEARCH_2026-07-30.md`。

## M6：资产拆分工作台

状态：完整候选完成，等待真实项目人工试用

正式大版本：`v0.7.0-asset-breakdown`

- 第三个内置工作区 `scenelens.asset_breakdown`。
- 主场景原画与补充参考只读导入、SHA-256 和独立项目包。
- 场景类型配置、严格资产清单 Schema、层级、复用组、制作优先级和证据边界。
- 原图归一化矩形、GrabCut 可见像素近似遮罩和用户移动/缩放。
- 用户新增、改名、分类、父子层级、拆分、合并、删除和持久保存。
- 只生成勾选资产；独立概念图、保守遮挡补全图与评审展示图。
- 单项失败不删除成功项；支持取消、有限重试、主动发送和系统凭据。
- 本地资产展示板、单项图片和结构化清单导出。
- 不捆绑 PyTorch、SAM 2、Grounding DINO、Florence-2、CUDA 或模型权重。
- 不把 AI 推断、不可见背面或生成补全伪装成原画事实。

验证记录：`M6_ASSET_BREAKDOWN_VALIDATION.md`。

### 0.7.1 维护更新

- Gemini 默认模型更新为 `gemini-3.6-flash`。
- 移除已不再向新用户开放的 `gemini-2.5-flash`。
- 404/503 使用有界双备用链，发送前展示并记录完整模型路由。
- 不改变 M6 数据结构、资产拆分范围或离线能力。

### 0.7.2 维护更新

- 对 AI 资产清单的悬空父级、重复 ID、循环层级、越界区域和无效关系进行
  可审计的本地结构修复。
- 结构修复不触发额外联网请求，不改变 AI 的美术语义。

### 0.8.0 全自动资产板

- 完成与人工校正流独立的全自动场景拆分、逐项生成和资产板合成。
- 完成 Nano Banana 2、Pro、Lite 当前模型选项与输出分辨率契约。
- 完成批量调用熔断、真实错误展示、部分成功保存和失败审计。
- 完成资产项目格式 v2、迁移前备份和全自动运行恢复。
- 完成撤销/重做、列表多选、画布 Shift 框选和删除快捷键。
- 当前不加入本地开放词汇检测、实例分割、CUDA 或第三方动态模型下载。

### 0.8.1 维护更新

- 修复 Gemini/Nano Banana 原始 REST 的图像尺寸与宽高比枚举。
- 可校正拆分和全自动资产板双向同步视觉分析、图片生成、Key 与分辨率设置。
- 不改变两种流程的数据隔离、主动发送、费用确认和原图只读边界。
- 不把图片生成模型误用为结构化资产清单分析器。

### 0.9.0 资产拆分提示语

- 增加与可校正拆分、全自动资产板并列的提示语工作流。
- 支持 AI 初稿、多轮协商、可选重新附图、手动编辑和历史版本。
- 支持复制中文、英文或完整提示语给外部生图工具。
- 资产项目格式升级到 v3，旧项目迁移前备份。
- 提示语只调用视觉/结构化文字输出能力，不调用图片生成能力。
- 当前不自动替用户登录或操作外部生图网站，也不判断外部模型最终出图质量。

## M7：离线交换与知识沉淀

- 完善隐私可控的审阅包导出和严格 JSON 文件/粘贴导入。
- 增加 Source Document、审阅模板、历史证据检索和报告编排。
- 不以 Markdown 自由解析替代正式交换 Schema。

## M8：Windows Alpha

- 干净 Windows 机器验收。
- 用户可理解的错误与日志打包。
- 完整第三方许可证清单。
- 中世纪村庄案例端到端试用。
