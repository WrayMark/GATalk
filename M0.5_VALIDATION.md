# M0.5 技术验证记录

状态：已通过人工验收并冻结  
工程验证日期：2026-07-18  
人工验收日期：2026-07-19

## 1. 验证环境

- Windows 10/11 x64（构建环境报告为 Windows 10.0.26200）
- Python 3.11.9 x64
- PySide6 / Qt 6.11.1
- 依赖版本见 `THIRD_PARTY_LICENSES.md` 和 `requirements-lock.txt`
- 工作区路径本身包含中文和空格

## 2. 验证结论

M0.5 的最小纵向流程已经打通：

> 读取图片 → EXIF/ICC 处理 → 双画布显示 → 后台分析 → 明度图/色板/直方图
> → PyInstaller `onedir`

目前没有证据表明 PySide6、Pillow、OpenCV Headless、NumPy 和 Colour
Science 之间存在阻止正式架构继续使用的冲突。用户已使用真实图片完成
人工试用，未发现影响使用的问题，并确认 M0.5 通过验收。

## 3. 已完成项目

- [x] PySide6 主窗口启动
- [x] 中文路径与空格路径
- [x] PNG、JPG/JPEG、WebP
- [x] EXIF 方向修正
- [x] 嵌入 ICC 到 sRGB 转换
- [x] 2K/4K 图片读取与分析
- [x] 双画布同步缩放和平移
- [x] A/B 单图切换与空格快捷键
- [x] 灰度、可调模糊、三阶/五阶明度
- [x] 8 色 Oklab 色板与面积比例
- [x] 明度直方图
- [x] 图片读取和分析离开 GUI 线程
- [x] 125% 和 150% 缩放离屏渲染
- [x] PyInstaller `onedir` 构建
- [x] 打包 EXE 启动及核心算法烟测
- [x] 关闭程序不写入或修改源图片

## 4. 自动化测试

- 19 项测试通过。
- 覆盖线性 sRGB 明度、直方图、三/五阶归纳、高斯模糊。
- 覆盖 Oklab 色板确定性、面积比例、单色退化和 4K 有界采样。
- 覆盖中文空格路径、四种格式、EXIF 方向、ICC 和源文件字节不变。
- 覆盖窗口创建、后台导入分析、灰度显示、视图同步和 A/B 可见性。

## 5. 4K 性能快照

测试图：3840 × 2160 RGB，当前开发机器。

| 操作 | 观测时间 |
|---|---:|
| 明度直方图 + 8 色 Oklab 色板 | 约 0.85 秒 |
| `sigma=8` 模糊 + 五阶明度 | 约 0.72 秒 |

色板采样被限制在 60,000 像素以内；上述任务由线程池执行，不在 GUI 线程
直接运行。时间仅代表当前机器快照，不是所有电脑的性能承诺。

## 6. 打包结果

- 构建方式：PyInstaller 6.21.0 `onedir`
- 入口：`dist/SceneLens/SceneLens.exe`
- 当前目录体积：约 264 MB
- 打包后的内部烟测实际执行 Oklab 聚类、OpenCV 模糊和灰度处理，退出码 0

初次使用 `--collect-all colour` 会收集无关测试模块，已经改为显式
`SceneLens.spec` 并排除 pytest、SciPy 和 Matplotlib。

## 7. 可以进入正式架构的模块

- `imaging.loader`：可作为正式图片读取边界继续演进。
- `analysis.luminance`：基础公式与测试可以保留。
- `analysis.palette`：技术路径可保留，色板美术可读性仍需真实案例校准。
- `ui.image_canvas`：双画布和归一化同步方案可进入下一轮迭代。
- `ui.workers`：后台任务边界可以保留，M1 后需增加取消与进度语义。
- `SceneLens.spec`：可作为 Alpha 打包基线。

## 8. 人工验收

- [x] 用户使用真实图片完成核心流程试用。
- [x] 用户未发现影响使用的问题。
- [x] 用户确认 M0.5 通过人工验收，可以冻结并进入 M1。

以下事项不阻止 M0.5 冻结，但仍需在后续发布验证中保留：

- 125%/150% 已通过离屏渲染，仍需在真实高 DPI 显示器上操作确认。
- `onedir` 在当前开发机运行成功；尚未在完全没有 Python 的干净虚拟机验证。
- 当前 264 MB 包体积可接受于技术原型，但不是最终发行目标。
- 三阶/五阶当前使用等间隔阈值；V0.1 的手动阈值控制尚未实现。
- 同镜头历史版本的透明叠加和滑块属于正式 V0.1，不在 M0.5。

## 9. 阶段结论

M0.5 的工程验证和真实图片人工试用均已完成。用户于 2026-07-19 确认
没有发现影响使用的问题，并批准冻结该阶段。M0.5 基线进入只修复阻断性
回归问题的维护状态，后续功能开发转入 M1A。
