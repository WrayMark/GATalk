# AI 资产清单引用修复验证记录

候选版本：0.7.2  
日期：2026-07-30

## 故障结论

AI 返回的 `assets[0].parent_asset_id` 指向本次清单中不存在的资产。JSON 与
字段类型正确，但跨资产引用不成立。该问题来自模型输出波动；SceneLens 旧版
把可修复的引用错误当成致命错误，导致整份结果无法使用。

## 修复行为

- 不存在的父级：清空父级，保留资产。
- 重复资产 ID：为后续重复项添加确定性数字后缀。
- 自引用或循环父级：断开一条父级边。
- 越界归一化矩形：裁剪到图片范围。
- 不存在或指向自身的资产关系：移除该关系。
- 名称、分类、证据、推断和制作建议保持原样。
- 不追加联网请求，不额外消费 API 额度。
- 修复摘要显示在界面并保存到 AI 运行记录。

## 自动验证

- 缺失父级保留资产。
- 越界矩形裁剪后仍通过完整 Schema。
- 无效关系被移除。
- 重复 ID 变为唯一 ID。
- 循环父级被断开。
- 输入原始 AI 结果不被原地修改。
- 资产拆分原有保存、遮罩、生成和导出流程继续通过。
- 完整自动化测试：209 项通过。
- 源码与 Windows `onedir` 包内烟测退出码均为 0。
- `onedir`：228 个文件，268,918,884 bytes（约 256.5 MiB）。
- 主程序 SHA-256：
  `A1239634855F5B618C4D3072D83BFCEB16AB0DA49BA3452C9D2B37630F5EA8DE`。

## 人工复测

1. 完全退出旧版 SceneLens。
2. 启动本次 `0.7.2` 候选版。
3. 打开原资产拆分项目，重新主动执行 AI 拆分。
4. 若模型再次返回悬空父级，程序应显示“AI 清单已完成并修复结构”，而不是
   “AI 资产拆分失败”。
5. 在资产树中检查被取消父级的资产，并按实际制作关系手工设置父级。
6. 保存、关闭并重开项目，确认资产和修复后的层级仍然存在。
