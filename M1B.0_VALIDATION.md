# M1B.0 工程验证记录

状态：自动化、迁移与 Windows 构建验证通过  
日期：2026-07-19  
目标版本：`0.2.0a0`

## 1. 范围

- M1A 稳定回退点；
- 同项目跨进程单写者保护与只读打开；
- 模块化单体边界和分析器注册；
- 制作意图与参考图视觉简报字段数据模型；
- SQLite schema v1 → v2 迁移；
- 模块预设配置加载。

不包含 M1B.1 UI、共享色板、颜色遮罩、三阶明度比较和 M1B.2 区域功能。

## 2. 数据验证

- [x] schema v1 项目迁移前创建完整备份。
- [x] M1A Brief 原值迁入制作意图，合并的时间与天气不被猜测拆分。
- [x] 字段保存值、来源、可信度、依据、用户确认状态和更新时间。
- [x] 自动/算法/AI 字段不能覆盖用户填写、修订或确认字段。
- [x] 参考图视觉简报绑定资源 SHA-256。
- [x] 替换参考图后旧视觉简报标记为 `stale`。
- [x] 旧分析记录迁入模块和分析器身份，旧缓存仍可读取。

## 3. 并发验证

- [x] 真实子进程持有写锁时，第二个进程不能取得写权限。
- [x] 第二个进程可以只读打开并读取项目。
- [x] 只读存储接口拒绝写入。
- [x] 持锁进程退出后可重新取得写权限。
- [x] 只有残留锁元数据、没有操作系统锁时可安全恢复。

## 4. 自动化与打包

- [x] 原有 31 项测试继续通过。
- [x] 当前完整测试 40 项通过。
- [x] Windows `onedir` 构建成功，目录大小约 253.8 MiB。
- [x] 打包内部烟测退出码为 0。
- [x] 模块预设配置已被收集到 `onedir`。

## 5. 尚未替代的人工验收

- 真实 125%/150% 高 DPI 显示器操作。
- 完全未安装 Python 的干净 Windows 机器启动。

以上仍是 Windows Alpha 前验收项，不记为 M1B.0 已验证事实。
