# M1A 工程验证记录

状态：试用候选完成，等待用户人工验收  
日期：2026-07-19  
版本：`0.1.0a1`

## 1. 已实现闭环

> 项目与 Art Brief → Shot → 参考图与 Version → 分析记录
> → 保存并关闭 → 重开恢复 → 添加和切换历史 Version

当前实现包含：

- 新建、打开、保存项目；
- 最多 12 条最近项目；
- Project → Shot → Version；
- 11 项项目级 Art Brief；
- 原始图片字节复制、SHA-256 和内容寻址；
- `project.json` + SQLite schema v1；
- 自动保存、原子清单更新和保存失败 dirty 状态保留；
- schema 迁移、迁移前备份、失败回滚和高版本拒绝写入；
- 当前 Shot、Version、显示模式、A/B、同步视图、模糊和画布状态恢复；
- 数值分析记录、证据类型、可重建 artifact；
- 左侧项目导航、双画布和四类右侧面板。

## 2. 自动化测试

- 完整测试：31 项通过。
- M0.5 原有 19 项全部继续通过。
- 新增覆盖：
  - 中文和空格项目路径；
  - Art Brief、Shot、Version 保存与重开；
  - 原始字节和 SHA-256；
  - 相同字节资源去重；
  - 分析结果恢复与 artifact 删除后重建；
  - schema v0 → v1 迁移备份；
  - 高版本数据库拒绝写入；
  - 迁移失败事务回滚和备份保留；
  - 清单原子替换失败时保留旧文件；
  - 自动保存失败时保留 dirty 状态；
  - UI 创建、关闭、重开、追加 Version 和切回历史 Version。

## 3. Windows `onedir`

- PyInstaller：6.21.0
- 输出：`dist/SceneLens/SceneLens.exe`
- 目录大小：253.8 MiB
- 已确认打包 `_sqlite3.pyd` 和 `sqlite3.dll`
- 打包烟测退出码：0

打包内部烟测实际执行：

- Oklab 色板、灰度和 OpenCV 模糊；
- SQLite 项目创建；
- Shot、参考图和 Version 导入；
- 原始资产复用；
- 分析记录保存；
- 项目关闭后的数据库重开和分析结果恢复。

## 4. 视觉检查

- 已在 1500 × 900 离屏环境渲染项目状态。
- 左侧 Project/Shot/Version 树、双画布和右侧四类面板没有发现布局阻断。
- 右侧面板可通过 splitter 调整宽度。
- 项目图片分析信息显示用户原文件名，不暴露内容寻址哈希文件名。

离屏检查不能替代真实 125%/150% DPI 显示器操作。

## 5. 人工试用步骤

建议按以下顺序验收：

1. 双击 `dist/SceneLens/SceneLens.exe`。
2. 新建“中世纪村庄”项目。
3. 打开左侧 Art Brief，填写“清晨薄雾”等项目目标。
4. 创建“村口固定机位” Shot。
5. 导入概念参考和当前 UE 截图。
6. 切换灰度、模糊、A/B，并调整缩放和平移。
7. 等待分析完成，按 `Ctrl+S`，关闭软件。
8. 从“文件 → 最近项目”重开，确认状态和分析结果恢复。
9. 导入第二张当前截图，确认生成 V2。
10. 在左侧 V1/V2 之间切换，确认历史图片和分析结果可查看。

## 6. 仍未确认

- 尚未由用户使用真实项目完成 M1A 人工试用。
- 尚未在完全无 Python 的干净 Windows 虚拟机运行。
- 尚未在真实 125%/150% DPI 显示器操作确认。
- 同一项目被多个 SceneLens 进程同时写入的互斥保护尚未实现；试用期间不要
  同时打开同一项目。
- schema 迁移框架已用合成 v0 数据库验证，但目前没有历史正式项目可迁移。
- M1B 色板遮罩、明度比较、色板配对和矩形成对区域尚未启动。

## 7. 阶段结论

M1A 已达到交付试用条件，但尚未通过用户人工验收。按既定顺序，在用户确认
M1A 通过前只修复 M1A 阻断性问题，不进入 M1B。
