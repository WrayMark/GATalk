# M6 资产拆分工作台验证记录

候选版本：0.7.0  
日期：2026-07-30

## 已完成

- 修复作品研究结果因 `MAX_TOKENS` 截断而无法解析 JSON 的问题。
- 新增第三个首页工作区“资产拆分工作台”。
- 完成主原画、补充参考、结构化资产清单、层级、复用和制作优先级。
- 完成原图矩形、可见像素遮罩、移动缩放、用户新增、拆分、合并、删除和修订。
- 完成只生成勾选资产、取消、有限重试、部分成功保留和生成记录。
- 完成单图、资产展示板和结构化 JSON 清单导出。
- 完成项目保存、重开恢复、原始字节与 SHA-256、操作系统写锁。
- 旧场景控制、作品研究和既有项目格式未迁移、未改写。

## 自动验证

- 作品研究长度截断修复：完整旧套件 190 项通过。
- 新模块加入后的当前完整套件：204 项通过。
- 覆盖归一化坐标、遮罩边界、用户优先、拆分合并、中文/空格路径、原始字节、
  EXIF 纠正尺寸、写锁、Schema、Mock、部分生成成功保留、展示板、清单导出、
  首页和窗口恢复。
- 两种场景夹具：历史聚落和科幻工业；验证分类差异、手工校正和重开持久化。
- 实际运行截图保存在
  `.qa/asset-breakdown-validation/asset-breakdown-village.png` 和
  `.qa/asset-breakdown-validation/asset-breakdown-industrial.png`；可用
  `scripts/render_asset_breakdown_smoke.py` 重建。
- 源码 `--smoke-test` 和打包内 `--smoke-test` 退出码均为 0。
- Windows `onedir` 共 228 个文件、268,915,419 bytes（约 256.5 MiB）。
- 主程序 SHA-256：
  `A67359E33BE7AFD02E890CC7B3BFAB02EA19E4084CCF77A0F77FE4B02E0411CC`。
- Word 手册通过 ZIP 完整性、157 个段落、27 个标题和关键内容检查；在 WPS
  实际打开后首页标题、版本、中文正文和标题层级显示正常。

## 验证边界

自动化中的两种场景是可复现的复杂合成夹具，用于验证软件工作流，不证明任一
远程 VLM 的真实语义质量。内置图像生成服务在本轮准备验证素材时发生网络错误，
没有改用需要额外 API Key 的命令行路径。

以下项目必须由用户使用自己的 Key 主动验证，不应伪记为已完成：

- 不同供应商对真实复杂原画的资产分类、模块套件和复用关系质量；
- 图片服务对遮挡补全和资产身份保持能力；
- 供应商账号权限、区域、模型可用性、费用和线上响应字段。

高 DPI 真机和完全无 Python 的干净 Windows 机继续保留为 Windows Alpha 项。
本机缺少 LibreOffice，Microsoft Word 自动导出 PDF 无响应，桌面逐页控制授权
随后超时，因此本轮未完成自动逐页图片渲染；不能把该项记为通过。

## 人工试用步骤

1. 启动 SceneLens，在首页进入“资产拆分工作台”。
2. 新建项目，选择场景类型并填写制作目标。
3. 导入复杂场景原画，可再导入最多三张补充参考参与 AI 拆分。
4. 选择视觉供应商和模型，查看发送清单并确认。
5. 检查资产分类、父子层级、重复组、优先级和原图区域。
6. 进入“框选资产”，新增遗漏项；移动区域，修改名称、分类、层级和说明。
7. 选中两项测试拆分/合并；关闭并重开项目确认修订仍在。
8. 勾选少量资产，选择图片供应商与生成类型，确认发送。
9. 检查成功、失败和取消后已完成项是否保留。
10. 导出清单、单项图片与 `asset_board.png`，核对来源标签。
11. 确认原始图片字节和位置未改变。
