# SceneLens M1B.2 技术与验收记录

状态：真实项目人工试用通过并冻结
日期：2026-07-19

## 人工验收结论

用户已使用真实项目完成 M1B.2 试用，并于 2026-07-19 明确确认通过。成对区域
创建、编辑、配对、局部明度与色彩分析、共享色板组成、区域颜色遮罩、
Version 复制及保存恢复未发现影响进入下一阶段的问题。

本结论只证明本次真实项目工作流通过，不替代尚未执行的真实 125%/150% 高 DPI
显示器体验和完全无 Python 干净 Windows 机器启动测试；两项继续保留为
Windows Alpha 前验收项。

## M1B.2A 已完成

- schema v4 区域、区域对和区域分析记录表。
- schema v3 → v4 迁移前数据库和清单备份。
- EXIF 纠正工作图上的归一化矩形坐标。
- 参考区域绑定 Shot、当前区域绑定具体 Version。
- 区域创建、命名、自定义语义、移动、八向手柄缩放和删除。
- 参考/当前区域一对一配对、待配对状态和同色编号。
- 区域与配对保存、关闭重开恢复。
- 复制上一 Version 区域；参考侧复用，当前侧生成独立记录。
- 普通查看模式与区域模式分离，Esc 退出区域模式。
- 区域叠层显示/隐藏。

## 自动验证

M1B.2A 完成时共 62 项测试通过。新增覆盖：

- 归一化坐标在不同图片/显示尺寸下保持一致；
- EXIF 方向纠正后的坐标映射；
- 中文区域名称与自定义语义保存；
- 区域配对、删除、重开恢复；
- 参考区域与具体 Version 当前区域的关系；
- 跨 Version 复制及新旧记录独立；
- 几何变化后分析标记过期；
- 画布当前缩放下的创建、移动和缩放交互；
- schema v3 → v4 迁移与迁移前备份；
- M0.5、M1A、M1B.1 完整回归。

## M1B.2B 已完成

- 明度、Oklab、彩度、色相与三阶比例统计。
- 低彩度过滤和圆形色相统计。
- 全图共享色板在区域中的组成比例。
- 详细比较界面和区域内颜色来源遮罩。
- 图片哈希、区域几何、明度阈值、共享色板、分析器版本和参数变化后的过期状态。
- 后台分析和快速 Version 切换后的旧任务隔离。

## 完整自动验证与性能

- 完整自动化测试：70 项通过。
- 4K（3840 × 2160）双方典型矩形区域分析：当前开发机约 0.98 秒。
- 色彩相关统计有界采样上限默认为每侧 250,000 像素；全区域明度统计不降采样。
- 固定共享色板中心在双方区域内的比例分别接近 100%。
- Windows `onedir` 构建成功，目录约 253.9 MiB。
- 打包内部烟测退出码为 0，覆盖图片分析、schema v4 项目、区域创建/配对、
  区域分析保存与恢复。
- PyInstaller 警告审查只发现 Windows 不使用的 `fcntl` 和 Colour Science
  的可选绘图/科学计算能力；候选版实际使用路径已由打包烟测覆盖。

性能数字只代表当前开发机，不推断所有 Windows 设备都能达到相同耗时。

## 已执行的真实项目人工试用步骤

1. 先备份真实项目，使用候选版打开；确认 v3 项目迁移后 `backups` 中生成迁移前备份。
2. 打开包含参考图和至少两个截图 Version 的 Shot，确认制作意图、参考图视觉简报、
   共享色板、全图遮罩和三阶明度没有回退。
3. 在“对比分析”页进入区域模式；分别框选天空、主体、前景、中景或远景中的一组。
4. 为双方区域命名并建立配对；观察同色编号、双方高亮和待配对状态是否清楚。
5. 移动并用手柄调整矩形；确认拖动流畅，松开后才开始分析，且画面和原图未被修改。
6. 检查平均线性明度、Oklab L/a/b、标准差、P10/P50/P90、有效跨度、三阶比例、
   彩度、中性色比例、色相分布和共享色板组成。
7. 点击区域共享色板的一行；确认颜色只在双方当前区域内高亮，区域外降低强度，
   按 Esc 能退出。
8. 调整三阶明度阈值；确认旧区域结果先标记过期，随后完成重新分析。
9. 保存并关闭，再次打开项目；确认区域、配对、参数和分析结果恢复。
10. 切换到没有区域的新 Version，使用“复制上一版本”；确认软件提示检查位置，
    且调整新 Version 区域不会影响旧 Version。
11. 使用一张 4K 截图重复主体区域分析，观察 UI 是否冻结或出现明显不可接受的延迟。

候选包入口：`dist/SceneLens/SceneLens.exe`。

## 已知限制

- 仅支持矩形和一对一配对；不支持多边形、套索、自动分割或多参考图。
- 删除使用确认对话框，尚未提供通用撤销栈。
- 色彩、色相和共享色板组成使用有界空间采样；采样上限记录在分析参数中。
- 当前不生成“过亮、太灰、更好、更差”等美术结论。

## Windows Alpha 前保留项

- 真实 125% / 150% 高 DPI 显示器体验。
- 完全未安装 Python 的干净 Windows 机器启动。

以上两项尚未执行，不标记为已通过。
